# G. 安全设计方案

> 版本：v1.0 | 全链路安全体系 | 只识别不操控原则

---

## 一、安全架构总览

```
┌─────────────────────────────────────────────────────────────┐
│                      客户端安全层                            │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐   │
│  │ 反调试    │ │ 反抓包    │ │ 反篡改    │ │ root/越狱检测  │   │
│  │ 混淆加固  │ │ 证书锁定  │ │ 完整性校验  │ │ 模拟器检测     │   │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────┘   │
├─────────────────────────────────────────────────────────────┤
│                      通信安全层                              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐   │
│  │ HTTPS    │ │ WSS(TLS) │ │ 请求签名   │ │ 防重放(Nonce) │   │
│  │ 1.2+     │ │ 加密传输  │ │ HMAC-SHA256│ │ 时间窗±5min   │   │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────┘   │
├─────────────────────────────────────────────────────────────┤
│                      网关安全层 (API Gateway)                 │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐   │
│  │ 频率限制  │ │ JWT鉴权   │ │ IP黑名单  │ │ 参数校验      │   │
│  │ 4层限流   │ │ RBAC     │ │ 地理围栏  │ │ SQL注入防护   │   │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────┘   │
├─────────────────────────────────────────────────────────────┤
│                      服务层安全                               │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐   │
│  │ 数据加密  │ │ 操作审计  │ │ 权限隔离  │ │ 敏感数据脱敏  │   │
│  │ AES-256  │ │ 全量日志  │ │ 最小权限  │ │ 日志/导出     │   │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────┘   │
├─────────────────────────────────────────────────────────────┤
│                      基础设施安全                             │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐   │
│  │ K8s网络   │ │ 密钥管理  │ │ 备份加密  │ │ 入侵检测      │   │
│  │ NetworkP │ │ Vault    │ │ AES-256  │ │ Falco/Syslog  │   │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、身份认证与鉴权 (JWT + RBAC)

### 2.1 认证流程

#### 2.1.1 玩家登录流程

```
客户端                          服务端                         存储
  │                              │                              │
  │  ① POST /auth/login          │                              │
  │  {phone, password, device}   │                              │
  │ ──────────────────────────>  │                              │
  │                              │  ② 校验账号密码              │
  │                              │  ③ 检查账号状态(冻结/封禁)    │
  │                              │  ④ 记录设备指纹             │
  │                              │  ⑤ 生成 Access Token         │
  │                              │  ⑥ 生成 Refresh Token        │
  │                              │  ⑦ 存储RT到Redis(7天)       │
  │                              │                              │
  │  ⑧ {access_token, refresh    │                              │
  │     _token, expires_in}      │                              │
  │ <────────────────────────── │                              │
  │                              │                              │
  │  ⑨ 本地存储(AT SecureStorage)│                              │
  │     (RT encrypted SharedPreferences)                        │
  │                              │                              │
```

#### 2.1.2 Token 规范

| 属性 | Access Token | Refresh Token |
|------|-------------|---------------|
| 格式 | JWT (RS256) | Opaque随机串 |
| 有效期 | **2 小时** | **7 天** |
| 存储 | 内存 + SecureStorage | 加密SharedPreferences |
| 包含信息 | uid, role, permissions, device_id | 仅 rt_id 映射 |
| 签发方 | auth-service | auth-service |

#### 2.1.3 JWT Payload 结构

```json
{
  "sub": "10001",
  "uid": "10001",
  "role": "player",
  "permissions": ["game:play", "room:create", ...],
  "device_id": "a1b2c3d4e5f6",
  "iat": 1703980800,
  "exp": 1703988000,
  "jti": "uuid-unique-token-id"
}
```

### 2.2 刷新 Token 流程

```
客户端                    API Gateway           Auth Service        Redis
  │                           │                       │               │
  │ ① AT过期,请求返回401       │                       │               │
  │<──────────────────────────│                       │               │
  │                           │                       │               │
  │ ② POST /auth/refresh      │                       │               │
  │  {refresh_token}          │                       │               │
  │ ───────────────────────>  │                       │               │
  │                           │ ───────────────────>  │               │
  │                           │                       │ ③ 查询RT有效性  │
  │                           │                       │ ④ 校验设备匹配   │
  │                           │                       │ ⑤ 生成新AT+RT对 │
  │                           │                       │ ⑥ 废旧RT        │
  │                           │                       │               │
  │ ⑦ {new_at, new_rt}        │                       │               │
  │ <──────────────────────────│ <─────────────────── │               │
  │                           │                       │               │
```

### 2.3 管理后台认证

| 差异点 | 玩家App | 管理后台 |
|--------|---------|----------|
| 登录方式 | 手机号+密码/微信 | 账号+密码+验证码(Google Authenticator) |
| AT有效期 | 2小时 | **30分钟**(更短) |
| RT有效期 | 7天 | **12小时** |
| 并发限制 | 同设备3个 | 同账号**仅1个**(踢出前一个) |
| MFA | 可选 | **强制**(管理员必开) |
| IP白名单 | 无 | **可选配置** |

### 2.4 WebSocket 认证

```typescript
// WS连接时在第一个消息中携带Token
// 客户端发送:
{
  "type": "auth",
  "token": "eyJhbGciOiJSUzI1NiIs...",
  "ts": 1703980800,
  "nonce": "random-string"
}

// 服务端响应:
{
  "type": "auth_result",
  "code": 0,
  "uid": "10001",
  "permissions": [...],
  "server_ts": 1703980801
}

// auth失败后服务端主动断开WS连接(1008 policy violation)
```

---

## 三、接口级安全

### 3.1 请求签名机制

**适用范围**: 所有涉及积分/房卡/创建房间的写操作接口

**签名算法**: HMAC-SHA256

**签名生成规则**:
```
sign = HMAC-SHA256(
  key = app_secret,           // 每渠道分配独立secret
  msg = method + path + ts + nonce + body_json_sorted
)
```

**请求头**:

| Header | 值 | 说明 |
|--------|-----|------|
| X-Timestamp | Unix秒级时间戳 | ±5分钟窗口有效 |
| X-Nonce | UUIDv4 | 一次性，5分钟内不可重复 |
| X-Sign | 64位hex签名 | 见上方算法 |
| X-App-Version | "2.3.0" | 版本号 |
| X-Device-ID | 设备指纹 | 用于风控关联 |
| X-Platform | android/ios/h5 | 平台标识 |

**服务端校验顺序**:
1. 时间戳是否在 ±5 分钟窗口 → 否则返回 `ERR_EXPIRED_REQUEST`
2. Nonce 是否已使用(Redis SETNX, TTL=5min) → 是则返回 `ERR_REPLAY_ATTACK`
3. 重新计算签名并比对 → 不匹配则返回 `ERR_INVALID_SIGNATURE`
4. 全部通过 → 放行

### 3.2 接口加密 (敏感数据)

**需要加密的请求体字段**:

| 接口 | 加密字段 | 算法 |
|------|---------|------|
| POST /user/register | password | bcrypt(hash) + RSA(transport) |
| POST /auth/login | password | 同上 |
| POST /wallet/transfer | amount, target_uid | AES-256-GCM |
| POST /safebox/open | safebox_password | SHA-256(hash) |
| POST /payment/create | pay_password | bcrypt + RSA |

**加密传输方案** (AES-256-GCM):
```
1. 客户端生成随机AES密钥(aes_key)和IV
2. 用aes_key加密明文得到ciphertext
3. 用服务端RSA公钥加密aes_key得到encrypted_key
4. 请求体: { data: ciphertext, key: encrypted_key, iv: iv }
5. 服务端用RSA私钥解密得aes_key, 再解密ciphertext
```

### 3.3 防刷与限流

#### 3.3.1 四层限流策略

```
                    ┌─ 全局限流 ──── 10,000 req/s (Nginx)
请求到达 ─────────> ├─ 渠道限流 ──── 500 req/s per channel (Gateway)
                    ├─ 用户限流 ──── 60 req/min per uid (Gateway)
                    └─ 接口限流 ──── 各接口独立配额 (Gateway/Service)
```

#### 3.3.2 关键接口限流配置

| 接口 | 限流维度 | 阈值 | 窗口 | 超限处理 |
|------|---------|------|------|---------|
| `POST /auth/login` | IP + account | 5次 | 15分钟 | 返回429,后续需验证码 |
| `POST /auth/sms-code` | phone | 1次 | 60秒 | 返回429 |
| `POST /sms-code` | IP | 10次 | 1小时 | 封IP 24小时 |
| `POST /room/create` | uid | 10次 | 1小时 | 返回429 |
| `POST /wallet/transfer` | uid | 20次 | 1小时 | 返回429 + 风控标记 |
| `POST /user/register` | IP | 5次 | 1小时 | 返回429 |
| `GET /replay/:id` | uid | 30次 | 1小时 | 返回429 |
| WS 连接 | IP | 3个并发 | — | 拒绝第4个 |
| WS 消息发送 | uid | 10条/秒 | 滑动窗口 | 丢弃超限消息 |

#### 3.3.3 滑动窗口实现 (Redis)

```python
# Redis Lua脚本 - 滑动窗口计数器
SLIDING_WINDOW_SCRIPT = """
local key = KEYS[1]
local window = tonumber(ARGV[1])
local limit = tonumber(ARGV[2])
local now = tonumber(ARGV[3])
local old_window = now - window

-- 清除过期的请求记录
redis.call('ZREMRANGEBYSCORE', key, '-inf', old_window)

-- 添加当前请求
redis.call('ZADD', key, now, now .. ':' .. math.random())

-- 统计当前窗口内的请求数
local count = redis.call('ZCARD', key)

if count > limit then
    return {0, limit - (count - limit)}  -- 被限制,剩余0
end
return {1, limit - count}  -- 通过,返回剩余配额
"""
```

### 3.4 SQL注入防护

| 防护层 | 方案 | 说明 |
|--------|------|------|
| ORM | 使用参数化查询 | Go: database/sql 占位符; MyBatis: #{param} |
| 输入校验 | 白名单正则 | 所有外部输入必须通过类型/格式校验 |
| 特殊字符转义 | 自动转义 | ORM层自动处理; 手写SQL禁止字符串拼接 |
| 字段白名单 | 动态排序/过滤只允许预定义字段 | ORDER BY 只接受枚举值 |
| WAF | SQL注入检测规则 | Nginx ModSecurity / API Gateway内置规则集 |

### 3.5 XSS防护

| 防护位置 | 方案 |
|----------|------|
| 客户端输入 | 所有用户昵称/签名等展示时自动 HTML 转义 |
| API输出 | Content-Type 严格设置; JSON不用HTML标签 |
| 管理后台(Vue) | Vue默认转义 {{}} ; v-html 禁止用于用户内容 |
| CSP策略 | 管理后台设置严格的 Content-Security-Policy |
| Cookie | HttpOnly; Secure; SameSite=Strict |

---

## 四、数据安全

### 4.1 敏感数据分类与存储

| 分类级别 | 数据示例 | 存储方式 | 展示脱敏 |
|----------|---------|---------|---------|
| **L1-绝密** | 支付密码 | bcrypt hash | ****** (永不展示) |
| **L1-绝密** | 保险箱密码 | SHA-256 + salt | ****** (永不展示) |
| **L1-绝密** | RSA私钥 | KMS/Vault保管 | 不落库 |
| **L2-机密** | 用户手机号 | AES-256加密存储 | 138****1234 |
| **L2-机密** | 身份证号 | AES-256加密存储 | 430***********123 |
| **L2-机密** | 银行卡号 | AES-256加密存储 | 6222********1234 |
| **L3-敏感** | 设备指纹 | 明文+索引 | 不对外展示 |
| **L3-敏感** | IP地址 | 明文 | 日志中脱敏为 192.168.*.* |
| **L4-一般** | 昵称/头像URL | 明文 | 正常展示 |

### 4.2 字段级加密实现

```sql
-- 示例: users表的phone字段加密存储
-- 明文: 13800138000
-- 存储格式: enc:AES256GCM:base64(iv+ciphertext+tag)

CREATE TABLE users (
    id BIGINT PRIMARY KEY,
    phone_encrypted VARCHAR(255) NOT NULL COMMENT 'AES-256-GCM加密手机号',
    phone_hash VARCHAR(64) NOT NULL COMMENT 'SHA-256哈希(用于唯一索引查重)',
    -- 其他字段...
    UNIQUE KEY uk_phone_hash (phone_hash)
);
```

**加解密工具类接口**:

```go
// core/crypto/aes.go
type FieldEncryptor interface {
    // 加密单个字段值
    EncryptField(plaintext string) (string, error)
    // 解密单个字段值
    DecryptField(ciphertext string) (string, error)
    // 批量加密结构体字段
    EncryptStruct(obj interface{}) error
    // 批量解密结构体字段
    DecryptStruct(obj interface{}) error
}
```

### 4.3 数据备份加密

| 备份类型 | 加密方式 | 密钥管理 | 保留期 |
|----------|---------|---------|--------|
| 全量DB每日快照 | pg_dump + gpg对称加密 | 离线密钥UKey | 30天 |
| 增量WAL归档 | AES-256-GCM | Vault动态密钥 | 7天 |
| 文件存储(OSS) | 服务端加密SSE-KMS | KMS主密钥 | 永久 |
| Redis RDB/AOF | LUKS加密卷 | — | 按需 |
| 日志归档 | gzip + gpg | 备份专用PGP密钥 | 90天 |

### 4.4 数据销毁策略

| 数据类型 | 逻辑删除 | 物理删除时机 |
|----------|---------|-------------|
| 用户账号 | is_deleted=1 | 注销180天后物理删除 |
| 聊天记录 | 标记隐藏 | 7天后物理删除 |
| 牌局详情 | 保留回放 | 回放保留90天后物理删除细节数据 |
| 积分流水 | **永不删除** | 合规存档(≥5年) |
| 日志 | — | 90天后轮转覆盖 |
| Token/Session | Redis TTL过期 | 即时 |

---

## 五、客户端安全

### 5.1 Android 安全加固

| 措施 | 方案 | 工具 |
|------|------|------|
| 代码混淆 | ProGuard/R8 混闭包名/方法名/变量名 | AGP内置 |
| DEX保护 | DEX2C / Packjiagui | 腾讯乐固/360加固 |
| 反调试 | ptrace自身 / 检测TracerPid | 自研SDK |
| 反Hook | 检测Frida/Xposed/Substrate端口 | 自研SDK |
| root检测 | Magisk/Su binary/BusyBox检测 | 自研SDK |
| 模拟器检测 | CPU传感器/蓝牙/GPS特征检测 | 自研SDK |
| 内存防Dump | mmap(MAP_PRIVATE) + mprotect | Native层 |
| 截屏保护 | Window FLAG_SECURE | 系统API |
| 证书固定(Certificate Pinning) | SSL pinning + fallback CAs | OkHttp/CertificatePinner |
| 完整性校验 | APK签名校验 + CRC校验 | 启动时检查 |

### 5.2 iOS 安全加固

| 措施 | 方案 |
|------|------|
| 代码混淆 | Swift名称混淆 + OC selector混淆 |
| 越狱检测 | 检测Cydia/apt/异常文件系统 |
| 反调试 | ptrace(PTRACE_DENY_ATTACH) / sysctl检测 |
| 反Hook | Frida检测(端口/线程/内存特征) |
| Keychain安全 | kSecAttrAccessibleWhenUnlockedOnly |
| ATS强制 | NSAllowsArbitraryLoads = NO + Certificate Pinning |
| 内存保护 | __DATA_CONST segment + 加密敏感段 |

### 5.3 通信层安全

```typescript
// 客户端网络层封装伪代码
class SecureHttpClient {
  private rsaPublicKey: string;
  private appSecret: string;

  async request(config: RequestConfig): Promise<Response> {
    // 1. 构造签名
    const ts = Math.floor(Date.now() / 1000);
    const nonce = crypto.randomUUID();
    const bodyStr = config.data ? JSON.stringify(sortKeys(config.data)) : '';
    const sign = hmacSha256(
      this.appSecret,
      `${config.method}${config.path}${ts}${nonce}${bodyStr}`
    );

    // 2. 设置安全Headers
    config.headers = {
      ...config.headers,
      'X-Timestamp': ts.toString(),
      'X-Nonce': nonce,
      'X-Sign': sign,
      'X-App-Version': getAppVersion(),
      'X-Device-ID': getDeviceFingerprint(),
      'X-Platform': getPlatform(),
    };

    // 3. 敏感接口加密Body
    if (ENCRYPTED_APIS.includes(config.path)) {
      config.data = await this.encryptBody(config.data);
    }

    return fetch(config.url, config);
  }
}
```

---

## 六、风控安全（只识别不操控）

### 6.1 核心原则

> **绝对禁止**: 服务端操纵牌序/发牌结果/随机种子来影响游戏公平性。
>
> **允许操作**: 识别异常后进行告警/标记/限制匹配/冻结账号等处置。

### 6.2 风控事件处理的安全约束

| 处理动作 | 是否影响牌局 | 审批要求 | 日志级别 |
|----------|------------|---------|---------|
| 标记观察 | 否 | 风控员自行决定 | INFO |
| 限制匹配 | 否(下一局生效) | 风控组长审批 | WARN |
| 冻结账号 | 是(强制离桌) | 需二级审批 | ALERT |
| 冻结保险箱 | 是 | 需二级审批 + 财务确认 | ALERT |
| 强制下线 | 是 | 需一级审批 | CRITICAL |
| 客服复核 | 否 | 自动转单 | INFO |
| 提交审计 | 否 | 自动流转 | INFO |

### 6.3 风控操作的审计日志

每次风控操作必须记录以下不可篡改的日志:

```json
{
  "event_id": "evt_20251201_abc123",
  "event_type": "risk_action",
  "operator_id": "admin_005",
  "operator_name": "张三(风控专员)",
  "target_user_id": "10001",
  "action_type": "freeze_account",
  "action_params": {"reason": "疑似团伙作弊", "duration_hours": 72},
  "risk_event_ids": ["evt_risk_001", "evt_risk_002"],
  "approval_chain": [
    {"approver": "admin_002(李四)", "role": "risk_leader", "action": "approved", "at": "2025-12-01T10:30:00Z"}
  ],
  "before_snapshot": {"user_status": "normal", "wallet_balance": 15000},
  "after_snapshot": {"user_status": "frozen", "wallet_balance": 15000},
  "client_ip": "10.0.1.55",
  "user_agent": "Chrome/120.0",
  "created_at": "2025-12-01T10:28:00Z"
}
```

---

## 七、安全监控与响应

### 7.1 安全告警规则

| 告警名称 | 触发条件 | 级别 | 响应动作 |
|----------|---------|------|---------|
| 暴力破解告警 | 单IP登录失败>20次/小时 | HIGH | 自动封IP 1h + 通知安全组 |
| Token异常刷新 | 单设备RT刷新>50次/小时 | MEDIUM | 标记设备可疑 + 强制重新登录 |
| 异地登录 | 账号从新城市登录(距离>500km) | MEDIUM | 推送通知 + 要求二次验证 |
| 积分异常变动 | 单笔转账>上限 或 1h累计>阈值 | HIGH | 冻结交易 + 触发风控复核 |
| 批量注册告警 | 单IP注册>5个账号/天 | HIGH | 标记IP + 新账号需人工审核 |
| 管理员异常操作 | 非工作时间批量查询/导出 | CRITICAL | 即时短信通知技术负责人 |
| SQL注入尝试 | WAF拦截SQL注入规则命中 | HIGH | 封锁来源IP + 记录攻击向量 |
| WS异常断线 | 同一UID短时间内频繁重连(>20次) | MEDIUM | 临时限速 + 标记异常 |

### 7.2 安全事件响应流程 (SLA)

```
P1-Critical (< 5分钟响应)
├── 数据泄露确认 / 生产环境被入侵 / 大规模资金异常
├── 响应: 立即召集安全应急小组 -> 隔离受影响系统 -> 通报甲方负责人
└── 恢复: 从备份恢复 / 漏洞修复 / 全量密码重置

P2-High (< 30分钟响应)
├── 单账号大额资金异常 / 批量账号被控 / DDoS攻击开始
├── 响应: 安全组介入 -> 冻结受影响账号 -> 分析攻击面
└── 恢复: 修补漏洞 / 加强策略 / 事后复盘报告

P3-Medium (< 2小时响应)
├── 个别异常行为标记 / 少量暴力破解尝试 / 可疑扫描探测
├── 响应: 风控组处理 -> 更新风控规则 -> 监控观察
└── 恢复: 规则优化 / 加入IP黑名单

P4-Low (< 24小时响应)
├── 低风险扫描 / 误报处理 / 策略调优建议
├── 响应: 记录分析 -> 排期优化
└── 恢复: 下一个迭代版本中改进
```

---

## 八、合规要求清单

| 合规项 | 要求 | 当前方案 | 状态 |
|--------|------|---------|------|
| 实名认证 | 用户注册时实名 | 手机号+实名API对接 | 待接入 |
| 防沉迷 | 未成年人限制 | 注册时身份证年龄判断 + 游戏时长限制 | 设计完成 |
| 数据留存 | 用户数据保存期限 | 积分流水≥5年, 牌局≥90天 | 已规划 |
| 数据出境评估 | 如涉及境外服务器 | 当前全部国内部署 | N/A |
| 隐私政策 | 收集声明+用户同意 | App内隐私协议弹窗 + 管理后台隐私页 | 待法务审核 |
| 支付合规 | 资金流水可追溯 | 完整积分流水链路 + 对账系统 | 设计完成 |
| 内容审核 | 公告/昵称过滤 | 敏感词库 + AI辅助审核 | 待集成 |
| 日志审计 | 操作可追溯 | 全量审计日志(不可删除) | 已设计 |

---

## 九、安全开发规范

### 9.1 开发阶段安全Checklist

- [ ] 所有外部输入经过校验(类型/长度/格式/白名单)
- [ ] 数据库操作使用参数化查询, 禁止字符串拼接SQL
- [ ] 敏感数据加密存储, 日志/导出中脱敏
- [ ] 接口具备适当的鉴权和权限校验
- [ ] 错误信息不暴露内部实现细节(堆栈/表名/IP)
- [ ] 密码使用bcrypt/scrypt/hashid, 禁止明文或MD5
- [ ] JWT/Token设置了合理的有效期
- [ ] 文件上传做了类型/大小/内容扫描
- [ ] 跨用户数据访问做了归属权校验(IDOR防护)
- [ ] 批量接口增加了分页限制(如 max 100条/页)

### 9.2 安全依赖管理

```bash
# 每日自动扫描依赖漏洞 (GitHub Dependabot / Snyk)
# 每周输出漏洞报告给开发团队
# 高危漏洞必须在48小时内修复或升级

# go.mod 中最低安全基线
go 1.21+

# 禁止的不安全依赖包示例(BAN LIST):
# - encoding/json (用 json-iterator 替代性能场景)
# - net/http 默认Client (需配置超时和TLS校验)
# - 直接exec/shell命令拼接用户输入
```

### 9.3 定期安全活动

| 活动 | 频率 | 参与人 | 产出 |
|------|------|--------|------|
| 代码安全审查 | 每次MR | 同行+安全接口人 | Review意见 |
| 渗透测试 | 每季度 | 第三方安全公司 | 渗透测试报告 |
| 依赖漏洞扫描 | 每日自动+每周人工 | DevOps+安全 | 漏洞清单 |
| 应急演练 | 半年一次 | 全团队 | 演练记录+改进 |
| 安全培训 | 每季度 | 全员 | 培训签到+考核 |

---

*文档结束*
