# E. 客户端技术设计

> 版本：v1.0 | Cocos Creator 3.x 横屏手游客户端

---

## 一、技术选型

| 项目 | 选型 |
|------|------|
| 引擎 | **Cocos Creator 3.8.x LTS** |
| 语言 | TypeScript (ES2020+) |
| UI | Cocos UI + 自定义组件 |
| 网络 | WebSocket(原生) + HTTP(xhr/fetch) |
| 音频 | AudioSource + WebAudio |
| 特效 | ParticleSystem2D + Spine/Lottie |
| 发布 | Android (APK) / iOS |

**选型理由**: Cocos 对2D横屏游戏支持最佳, 热更新原生友好, 社区棋牌项目丰富。

---

## 二、项目目录结构

```
project-root/
├── assets/                      # Cocos 资源
│   ├── scenes/
│   │   ├── Splash.scene        # 启动页(版本检测+资源下载)
│   │   ├── Login.scene
│   │   ├── Lobby.scene         # 大厅
│   │   └── games/
│   │       ├── taojiang/      # 每款游戏独立场景
│   │       │   ├── Entrance, Loading, CreateRoom,
│   │       │   ├── WaitingRoom, Table(★核心), RoundSettlement,
│   │       │   ├── RoomSettlement, RecordList, Replay
│   │       ├── hongzhong/, paodekuai/, changsha/,
│   │       └── yiyang-waihuzi/, yuanjiang-qianfen/
│   ├── prefabs/
│   │   ├── ui/(Button,Card,TileHand,DiscardArea,MeldArea,OpBtns,
│   │   │         Countdown,PlayerInfo,ChatBubble,SettlementCard,Toast)
│   │   └── vfx/(CoinBurst_L1~L5, WinBanner, MahjongHu,
│   │             PengWord, GangShockwave, BombExplosion)
│   ├── resources/
│   │   ├── textures/common/, mahjong-taojiang/tiles-realistic-retro/,
│   │   │          mahjong-hongzhong/, poker-paodekuai/cards-classic/,
│   │   │          zipai-yiyang-waihuzi/cards-traditional/
│   │   ├── fonts/, audio/(common,mahjong,poker,zipai)/
│   └── remote/                 # 热更新资源(CDN)
│       ├── audio/(mahjong,poker,zipai,common)/
│       ├── vfx/(common,mahjong,poker,zipai)/
│       ├── configs/(game-rules,audio-config,vfx-config)/
│       └── version-manifest.json
│
├── scripts/                     # TypeScript 代码
│   ├── main.ts                  # 入口
│   ├── Manager/
│   │   ├── GameManager.ts       # 游戏生命周期
│   │   ├── SceneManager.ts     # 场景跳转管理
│   │   ├── NetworkManager.ts   # 网络(HTTP+WS)
│   │   ├── AudioManager.ts    # 音效/BGM/语音/音量
│   │   ├── VFXManager.ts       # 特效(粒子/对象池)
│   │   ├── ResourceManager.ts  # 资源加载/缓存/热更新
│   │   ├── ConfigManager.ts    # 配置(本地+远程)
│   │   ├── StorageManager.ts   # 本地存储
│   │   └── DeviceAdapter.ts    # ★ 横屏适配器
│   ├── Network/
│   │   ├── HttpClient.ts, WsClient.ts, WsMessageHandler.ts,
│   │   ├── Protocol.ts, Reconnector.ts
│   ├── UI/
│   │   ├── BaseScene.ts, UIManager.ts, components/
│   ├── Games/                  # 各游戏纯逻辑(不依赖Cocos)
│   │   ├── base/(IGamePlugin,GameState)
│   │   └── taojiang/, hongzhong/, paodekuai/, changsha/,
│   │       yiyang-waihuzi/, yuanjiang-qianfen/
│   ├── Models/(User,Room,Wallet,Message...)
│   └── Utils/(ObjectPool,MathUtil,EncryptUtil,LogUtil)
```

---

## 三、核心架构模式

### 3.1 DeviceAdapter — 横屏适配器 (★ 核心)

```typescript
class AppLayoutAdapter {
  static readonly DESIGN_W = 1920;
  static readonly DESIGN_H = 1080;
  
  scaleFactor: number;           // 相对设计稿缩放比
  safeArea: { l,r,t,b };     // 安全边距(px)
  canvasSize: Size;
  
  // 布局区域(像素坐标, 已适配安全区)
  tableArea: Rect;             // 牌桌居中(等比缩放)
  handArea: Rect;              // 手牌区(底部)
  opBtnArea: Rect;            // 操作按钮(右下角)
  playerAreas: Map<number, Rect>; // 4方位玩家信息区
  
  init(): void {
    // 1. 读屏幕实际尺寸 + 安全区域
    // 2. scaleFactor = min(screenW/1920, screenH/1080) * 等比
    // 3. 计算各布局区域实际位置
    // 4. 应用到 Widget 布局组件
  }
  
  onResize(newSize): void { this.init(); EventTarget.emit('LAYOUT_RESIZED'); }
}
```

### 3.2 网络层

```
WsClient → WsMessageHandler → RoomHandler/GameHandler/TableController

WsClient:
- 连接建立/断开/重连(指数退避,最多5次)
- 心跳30s定时器
- seq编号 + missed_messages补发

HttpClient:
- REST API封装, Token自动附加/刷新
- 离线请求队列/重试
- 统一错误码处理
```

### 3.3 AudioManager — 音频分层

| 层 | 内容 | 默认音量 | 循环 |
|----|------|---------|------|
| BGM | 大厅/各牌桌背景音乐 | 35% | 是 |
| SFX | 按钮/摸牌/出牌/吃碰杠 | 80% | 否 |
| Impact | 胡/炸弹/自摸/结算 | 100% | 否 |
| Voice | 碰/杠/胡/要不起 | 90% | 否 |
| CoinFX | 金币奖励虚拟音效 | 90% | 否 |

- 音效池(ObjectPool), 预创建常用实例避免运行时new
- 后台自动暂停/前台恢复
- 来电中断处理

### 3.4 VFXManager — 特效分级

| 设备等级 | 条件 | 粒子量 | 帧率目标 |
|---------|------|--------|---------|
| LOW | 低端Android(<4GB RAM) | 30% | 30 FPS |
| MID | 普通/老iPhone | 60% | 45 FPS |
| HIGH | 主流机型 | 100% | 60 FPS |

- 全屏特效 ≤ 2.5s，必须可跳过
- 自动降级: 连续10帧<28FPS 或 内存超阈值
- 对象池:每种特效预建Pool(size=3~5)

### 3.5 ResourceManager — 热更新

启动流程:
```
Splash Scene → 读本地版本号 → 请求版本检测接口
→ 强制更新? → 弹窗阻断
→ 热更新/资源更新? → 显示进度条 → CDN下载
→ 校验SHA256 → 解压到临时目录 → 校验通过后切换
→ 加载大厅场景

资源分类:
- **基础资源** (`assets/resources/`): 随App包, 不热更
- **游戏资源** (`assets/remote/{game}/`): 独立版本号, 进游戏前检查
- **配置资源** (`assets/remote/configs/`): 规则/音量/特效配置, 可热更
```

---

## 四、牌桌场景组件树 (以长沙麻将为例)

```
TableScene (Canvas, 1920x1080)
├── Background (全屏, 等比缩放可裁切)
│   └── TableBg (桌面居中, 等比缩放)
├── TopBar (顶部安全区内)
│   ├── RoomNo "886688" | RoundInfo "第3/8局" | RuleSummary
│   └── Wifi/Battery/Time | SettingsBtn ⚙️
├── LeftPlayerArea (左侧, 安全区)
│   └── Avatar + Nick + TilesCount("13张") + OnlineStatus
├── RightPlayerArea (右侧, 同上)
├── TopCenterArea (顶部中间)
│   └── 对家 Avatar + Nick + TilesCount
├── BottomArea (底部, 手势条上方)
│   ├── HandArea (★ 手牌横排, 核心交互区)
│   │   └── Tile[0]..Tile[13] (可选高亮/排序)
│   ├── OperationBtnGroup (右下角)
│   │   └── Chi/Peng/Gang/Hu/Pass/Hint (条件显示)
│   └── AutoPlayIndicator (托管状态)
├── CenterArea (中央公共区)
│   ├── DiscardArea (弃牌堆叠)
│   ├── MeldArea (明牌: 碰/杠展示)
│   └── LastActionDisplay ("A碰!7w" + 动画)
└── OverlayLayer (动态浮层)
    ├── TingHintPanel (听牌提示, 底部半透明弹层)
    ├── QishouHuPopup (起手胡全屏弹窗)
    ├── ZhongniaoAnimation (中鸟动画层)
    ├── CountdownRing (倒计时环)
    └── ChatBubbleContainer (左下角聊天)
```

---

## 五、数据流与状态管理

### 5.1 推荐模式: 轻量级响应式

不使用 Redux/Vuex 等重量级方案, 采用事件驱动+单例状态:

```
数据流向:
API/WS 数据 → Manager(单例) → Event emit → UI Component 更新

示例: 房间玩家列表更新
  WsMessageHandler 收到 room.player_joined 
    → RoomState.addPlayer(data) 
    → EventTarget.emit('ROOM:PLAYER_JOINED', playerData) 
    → TableScene 中 WaitingArea 监听事件 → 更新座位UI
```

### 5.2 关键单例状态

| 单例 | 存储内容 | 生命周期 |
|------|---------|---------|
| `UserState` | 当前用户信息(token/昵称/余额) | 登录→登出 |
| `RoomState` | 当前房间完整状态(players/round/config) | 加入→离开/断线 |
| `GameRuleCache` | 当前游戏的规则配置 | 进入游戏时加载 |
| `AudioConfig` | 用户音量设置(各层级开关) | 持久化到localStorage |
| `VFXConfig` | 特效等级设置 | 持久化 |
| `DeviceProfile` | 设备等级/分辨率/安全区 | 启动时检测 |
| `AppVersionState` | 各版本号(app/hotfix/res) | 启动/热更新后刷新 |

---

## 六、性能优化策略

### 6.1 启动优化

| 策略 | 说明 |
|------|------|
| 首屏直出 | Splash 场景极简, 异步加载后续 |
| 分步加载 | 先加载大厅必需资源, 游戏资源按需加载 |
| 资源预下载 | 在空闲时间(WiFi+充电)预下载下一游戏资源 |
| 缓存策略 | 已加载资源缓存在内存, 不重复IO |
|Skeleton | 大厅/牌桌先显示骨架屏, 图片加载完替换 |

### 6.2 运行时优化

| 策略 | 说明 |
|------|------|
| 对象池 | 所有特效/弹窗/卡片使用对象池, 禁止运行时 new/destroy |
| 纹理控制 | 牌面/粒子按设备等级限制数量 |
| 帧率锁定 | 目标60FPS, 低端机锁30FPS(减少渲染压力) |
| 不可见裁剪 | 离屏节点 setActive(false), 停止更新 |
| 纹理合并 | 多个相邻动画合并为一次 DrawCall |
| 字体缓存 | BMFont 预生成, 避免 runtime 创建 |
| 音频预载 | 常用音效 preload 到内存池 |
| GC 控制 | 避免频繁大对象创建/销毁, 定期触发 gc |

### 6.3 包体优化

| 策略 | 目标 |
|------|------|
| 资源压缩 | 图片压缩(WebP/ASTC), 音频(Ogg/Vorbis) |
| 按需加载 | 首次进入某游戏才加载该游戏资源包 |
| 代码混淆 | TypeScript → JavaScript 后 UglifyJS/Terser |
| 多渠道包 | 不同渠道仅包含不同配置和SDK |
| 分包加载 | 使用 AssetBundle 将游戏拆分为多个分包 |
| 资源CDN | 所有热更资源和图片走CDN, 不打在APK里 |
