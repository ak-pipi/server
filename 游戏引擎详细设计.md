# D. 游戏引擎详细设计

> 版本：v1.0 | 插件化架构 + 6款游戏算法 + 公共框架组件

---

## 一、引擎总体架构

### 1.1 分层架构

```
┌─────────────────────────────────────────────────────┐
│                   Game Engine Framework               │
│  ┌──────────┐  ┌──────────┐  ┌─────────────────────┐   │
│  │ Game     │  │ Replay   │  │ AI Player           │   │
│  │ Plugin   │  │ Builder  │  │ (托管/测试)         │   │
│  │ Interface│  │          │  │                      │   │
│  └────┬─────┘  └────┬─────┘  └──────────┬──────────┘   │
│       │             │                    │              │
│  ┌────▼─────────────▼────────────────────▼──────────┐  │
│  │              Core Engine Components              │  │
│  │  ┌─────────┐ ┌─────────┐ ┌──────────┐ ┌──────┐  │  │
│  │  │SeatMgr  │ │TurnTimer│ │ActionQueue│Discon│  │  │
│  │  └─────────┘ └─────────┘ └──────────┘ └──────┘  │  │
│  │  ┌─────────┐ ┌─────────┐ ┌──────────┐            │  │
│  │  │SecureRnd│ │ScoreLedg│ │RoomState │            │  │
│  │  └─────────┘ └─────────┘ └──────────┘            │  │
│  └───────────────────────────────────────────────────┘  │
│                         │                               │
│  ┌────────────────────▼───────────────────────────┐  │
│  │               Network / Transport Layer          │  │
│  │  WebSocket Handler → Message Router → State Sync │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

### 1.2 插件接口定义 (TypeScript 风格伪代码)

```typescript
// ============ 核心类型 ============

interface Tile {
  suit: string;    // w(万)/s(条)/c(筒)/z(风箭)/h(红中)/d(大字)/x(小字)/p(花)
  value: string | number;
  display: string; // "1w", "红中", "大王"
}

interface PlayerState {
  userId: bigint;
  seat: number;
  hand: Tile[];        // 手牌(仅自己可见完整, 其人只看数量)
  melds: Meld[];       // 明牌(吃碰杠偎跑提)
  discards: Tile[];   // 弃牌区
  isReady: boolean;
  isOnline: boolean;
  isAutoPlay: boolean;
  score: number;
}

interface Meld {
  type: 'CHI' | 'PENG' | 'GANG' | 'WEI' | 'PAO' | 'TI';
  tiles: Tile[];
  fromSeat?: number;
}

interface RoundContext {
  roundId: bigint;
  roomId: bigint;
  gameCode: string;
  ruleConfig: any;      // 该游戏的规则配置快照
  seed: string;        // 随机种子
  players: Map<number, PlayerState>; // seat -> state
  dealerSeat: number;
  currentSeat: number;
  stepCount: number;
  remainingTiles: Tile[];
  discardedTiles: Map<number, Tile[]>; // seat -> tiles
  wall: Tile[];        // 牌墙剩余
  status: 'WAITING' | 'DEALING' | 'PLAYING' | 'SETTLING' | 'FINISHED';
}

// ============ 游戏插件接口 ============

interface GamePlugin {
  readonly meta: GameMeta;
  
  // --- 生命周期 ---
  createGame(config: any): GameState;
  shuffleAndDeal(state: GameState): DealResult;
  
  // --- 操作校验 ---
  validateAction(state: GameState, action: GameAction): ValidationResult;
  getAvailableActions(state: GameState, seat: number): AvailableAction[];
  
  // --- 执行动作 ---
  applyAction(state: GameState, action: GameAction): ActionResult;
  
  // --- 胜负判断 ---
  checkWin(state: GameState, seat: number): WinResult | null;
  
  // --- 结算 ---
  settleRound(state: GameState): RoundSettlement;
  settleRoom(roundSettlements: RoundSettlement[]): RoomSettlement;
  
  // --- 回放 ---
  buildReplay(actions: GameAction[], context: RoundContext): ReplayData;
  recoverState(replayData: ReplayData, targetStep: number): Partial<GameState>;
  
  // --- 辅助 ---
  getHintActions(state: GameState, seat: number): GameAction[];
  getRuleConfigSchema(): JSONSchema;
  getTestCases(): TestCase[];
}

interface GameMeta {
  code: string;          // mahjong_taojiang
  name: string;          // 桃江麻将
  type: 'mahjong' | 'poker' | 'zipai';
  minPlayers: number;
  maxPlayers: number;
  tileSetDefinition: TileSetDef;
}
```

---

## 二、公共框架组件详细设计

### 2.1 SecureRandomManager — 安全随机数管理器

```
职责:
- 使用 crypto/rand (Go) 或 java.security.SecureRandom (Java) 生成真随机
- 每局生成唯一 seed (64字节随机数)
- 计算 SHA256 hash 作为 seed_hash 存入数据库(审计用)
- 加密存储 seed 明文 (AES-GCM), 仅审计时解密
- 不使用 Math.random() / rand() 等伪随机

API:
  generateSeed() → { seed: bytes, seedHash: string, seedEncrypted: string }
  shuffle<T>(array: T[], seed: bytes): T[]
  nextInt(seed, min, max): int
```

### 2.2 SeatManager — 座位管理

```
职责:
- 分配座位 (房主=0号座, 后续按加入顺序分配)
- 座位方位映射 (0=东/庄 1=南 2=西 3=北, 或自定义顺序)
- 下家/对家/上家计算
- 庄家轮转规则 (轮庄/赢者坐庄/房主首庄)

API:
  assignSeat(room, player) → seat
  getNextSeat(currentSeat, totalSeats) → seat
  getOppositeSeat(seat, totalSeats) → seat
  rotateDealer(currentDealer, rule, winner?) → newDealer
```

### 2.3 TurnTimer — 回合计时器

```
职责:
- 管理每个玩家的操作倒计时
- 超时自动触发 AUTO_PLAY / PASS
- 支持暂停(断线/解散投票期间)
- 时间配置: 基础15秒, 每次操作+3s(最大30s)

状态机:
  IDLE → COUNTDOWN(15s) → EXPIRED → onTimeout callback
                     ↓ CANCEL (收到合法操作)

数据结构:
  turnTimers: Map<seat, { deadline: timestamp, timerId }>
```

### 2.4 ActionQueue — 操作队列

```
职责:
- 保证同一玩家不会并发发送多个操作
- 处理多人同时操作的优先级 (HU > GANG > PENG > CHI > DISCARD)
- 序列化操作执行, 保证状态一致性

优先级规则(从高到低):
  P0: HU (胡牌) — 必须立即处理, 阻断其他操作
  P1: GANG (杠)
  P2: PENG (碰) / PAO (跑) / TI (提) / WEI (偎)
  P3: CHI (吃)
  P4: DISCARD (出牌) / PASS (过)

实现:
- 每个玩家的操作进入对应优先级队列
- 服务端每步只处理一个操作
- 如果同时有多人可操作, 按 seat 顺序依次处理
```

### 2.5 ScoreLedgerWriter — 积分流水写入器

```
职责:
- 所有积分变动通过此组件统一写入
- 保证事务性: 同一结算中的所有流水要么全成功要么全回滚
- 生成配对的 ref_ledger_id (A输B赢的流水互相关联)
- 支持冻结/解冻 (游戏中锁定积分)

流程:
1. beginTransaction()
2. for each player in settlement:
   a. 写入 wallet_ledger (change_amount 可正可负)
   b. 更新 wallets.balance (乐观锁 version + WHERE version=old_version)
   c. 如果余额不足或版本冲突 → rollback + 返回错误
3. commitTransaction()
4. 发布事件通知 (用于统计/风控监听)
```

### 2.6 DisconnectHandler — 断线重连处理器

```
职责:
- 检测到 WS 断开后保留玩家状态 (TTL=30s)
- 自动开启 AUTO_PLAY (托管模式)
- 记录断线时的完整状态快照
- 重连成功后下发 missed_messages
- 重连超时(30s)后触发解散投票

数据保留:
  disconnectSnapshots: Map<userId, {
    roomNo, seat,
    lastSeqReceived,
    gameStateSnapshot: RoundContext (JSON序列化),
    disconnectedAt: timestamp
  }>

清理定时任务:
  - 每10秒扫描一次
  - 超过30s的断开记录 → 移除, 触发 autoPlay 或 dissolve
```

### 2.7 DissolveVoter — 解散投票管理器

```
职责:
- 管理解散投票的生命周期
- 统计投票结果
- 超时未投视为同意
- 不同阶段有不同规则:

阶段          需要同意数    超时时间
─────────────────────────────────────
WAITING       过半即可     60s
READY         全部同意     30s
PLAYING      全部同意     30s (防止恶意打断)
SETTLING     不可发起      -

API:
  initiate(voterSeat, reason) → voteRecord
  vote(seat, agree) → result
  checkResult() → { dissolved, agreeCount, rejectCount }
  cancel() → 取消投票
```

### 2.8 ReplayBuilder — 回放数据构建器

```
职责:
- 在游戏过程中实时收集每一步操作
- 构建完整的回放数据结构
- 存入 MongoDB (game_replays collection)
- 支持按 step 快速跳转到任意状态

构建时机:
- 每次 action_result 广播时, 同时追加到当前回放的 actions 数组
- settlement 时完成回放元数据并异步持久化
```

---

## 三、各游戏算法流程

### 3.1 桃江麻将 (mahjong_taojiang)

**特点**: 2人麻将, 地方规则可配置

#### 流程图
```
创建房间 → 配置规则(人数/局数/底分/吃碰杠/庄家)
  ↓
等待第2位玩家加入 → 双方准备
  ↓
【发牌】每人13张, 庄家14张(已摸一张)
  ↓
【游戏循环】(循环直到有人胡或牌摸完)
  ├─ 庄家先出牌(或已摸的牌选择出/吃碰杠胡)
  ├─ 非庄家响应(可吃/碰/杠/胡/过)
  │   ├─ 有人胡 → 进入[结算]
  │   ├─ 有人碰/杠 → 该玩家继续出牌
  │   ├─ 有人吃 → 该玩家出牌
  │   └─ 都过 → 下家摸牌+出牌
  ├─ 摸牌 → 检查是否自摸/暗杠/补花
  │   ├─ 自摸胡 → [结算]
  │   ├─ 暗杠 → 补张牌 → 继续出牌
  │   └─ 正常 → 出牌(回到循环起点)
  └─ 牌墙耗尽(流局) → 本局结束, 庄家继续或轮转
  ↓
【单局结算】calculateBaseScore + calculateZimo/Dianpao/Gang/Banker/MaxLimit
  ↓
检查是否达到总局数? → 否 → 下一局
                  → 是 → [整房结算]
```

#### 核心算法模块
```text
桃江麻将引擎模块:

1. TileSetManager (牌集管理)
   - 支持: 万/条/筒/风牌/箭牌
   - 可选: 红中/赖子/花牌
   - 每种牌的张数可配置

2. HuChecker (胡牌判断)
   - 基本胡型: 4+N*3+2 (4组面子+1对将)
   - 特殊番型(可选配置): 七对、碰碰胡、清一色等
   
3. TingCalculator (听牌提示)
   - 枚举所有可能打出的牌
   - 对每种打出后的手牌检测是否可胡
   - 返回可胡的牌列表和番数

4. SettlementCalculator (结算)
   - calculateBaseScore() — 基础分(底分 * 番数系数)
   - calculateZimoScore() — 自摸加成
   - calculateDianpaoScore() — 点炮责任
   - calculateGangScore() — 杠分计算(明杠/暗杠/补杠不同)
   - calculateBankerScore() — 庄家输赢加减
   - calculateMaxLimit() — 封顶限制
   - writeScoreLedger() — 写入流水
```

### 3.2 红中麻将 (mahjong_hongzhong)

**特点**: 赖子系统复杂, 听牌/七对/碰碰胡多套算法

#### 核心难点: 赖子替代最优组合
```
问题: 给定一手含赖子的牌, 找出所有可能的胡牌组合, 
     并确定赖子的最佳替代方式使得番数最高

算法:
1. 先不考虑赖子, 用标准胡牌算法检测基础胡型
2. 对于每种基础胡型, 尝试用赖子替代某些位置:
   - 替代将牌(万能替代)
   - 替代组成面子
   - 替代组成将(如果允许红中做将)
3. 计算每种替代方案的番数得分
4. 返回得分最高的方案

数据结构:
  LaiziReplacement {
    originalTile: Tile,
    replacedWith: Tile,
    position: 'meld' | 'pair' | 'single',
    resultingPattern: PatternInfo,
    fanScore: number
  }

  TingResult {
    discardTile: Tile,           // 打出这张牌后可以听
    huPatterns: [
      { pattern: '七对', fan: 2, score: 8, laiziUsage: {...} },
      { pattern: '碰碰胡', fan: 4, score: 16, laiziUsage: {...} },
      { pattern: '普通胡', fan: 1, score: 4, laiziUsage: {...} }
    ]
  }
```

#### 引擎模块
```text
红中麻将引擎:

1. LaiziCalculator (赖子计算器)
   - setLaiziRules(count, asLaizi, canHuWithLaizi, replaceRule)
   - findBestReplacement(hand, laizi) → ReplacementPlan[]

2. HuChecker (增强版)
   - supportPatterns: ['3N+2', 'qiDui', 'pengPengHu']
   - withLaiziSupport: true

3. TingCalculator (听牌计算器)
   - 枚举27种打出可能(标准手牌+赖子变体)
   - 每种打出 → 检测所有可胡组合
   - 按番数排序返回

4. GangCalculator (杠分计算)
   - mingGang(明杠) / anGang(暗杠) / buGang(补杠) 不同计分
   - 杠上开花特殊处理

5. Settlement (结算)
   - 自摸加倍(zimoDouble)
   - 红中特殊计分(hongzhongBonus)
   - 杠上炮特殊处理
```

### 3.3 跑得快 (poker_paodekuai)

**特点**: 牌型校验严格, 出牌比较逻辑, 托管AI

#### 核心算法: 牌型比较
```
牌型层级(从低到高):
  单张 < 对子 < 三张 < 三带一 < 三带二 < 顺子 < 连对 < 飞机 < 炸弹 < 王炸

比较规则:
1. 先比牌型等级 (炸弹 > 其他一切非炸弹)
2. 同类型比主牌点数
3. 主牌相同比副牌(三带的单张/对子)
4. 特殊: 炸弹比点数(4444 > 3333)
5. 王炸最大(大小王)

出牌校验流程:
  validatePlay(currentHand, playedCards, leadSuit, leadType):
    1. playedCards 必须是 currentHand 的子集
    2. 如果是首出 → 检查出牌合法性(是否必须带黑桃3)
    3. 如果跟牌 → 必须压过上家(同类型更大 or 更高牌型)
    4. 检查是否符合 mustIncludeSpade3 规则
    5. 返回 valid/invalid + 错误原因
```

#### 托管 AI 策略
```
AutoPlayStrategy (跑得快托管):

简单模式(默认):
  - 有顺出顺, 有对出对
  - 能管必管, 不能管过
  - 优先出小牌, 保留大牌

进阶模式(可选):
  - 记忆已出的牌 (推算对手剩余)
  - 当自己只剩 ≤3 张时, 判断是否能一次出完
  - 如果搭档出了大牌, 自己配合出小牌压制下家
  - 对手快要出完时, 尝试卡住(不出其需要的牌)
```

### 3.4 长沙麻将 (mahjong_changsha)

**特色**: 起手胡检测(7种) + 中鸟结算

#### 起手胡检测算法
```
起手胡 = 发牌完成后立即检测是否满足特定牌型

检测函数:
checkQishouHu(hand_14tiles, rules) → QishouHuResult[]

检测项(按rules.config开关):
1. queYiSe (缺一色) — 手牌缺少一种花色
2. banBanHu (板板胡) — 14张全部由2-8的牌组成(无19/风箭)
3. daSiXi (大四喜) — 4组相同的刻子(如4个111)
4. liuLiuShun (六六顺) — 6组连续递增的牌
5. jieJieGao (节节高) — 相邻3组每组递增
6. sanTong (三同) — 3组完全相同的面子
7. yiZhiHua (一枝花) — 全部由同一种序数牌组成
8. zhongNiao (中鸟) — 特定牌命中(结算阶段)

实现:
- 每种起手胡独立检测函数
- 可以叠加多种(如缺一色+板板胡)
- 返回所有触发的类型和各自番数
```

#### 中鸟结算
```
ZhongNiaoSettlement:
输入: 最后一张翻开的鸟牌, 各玩家信息
输出: 每个命中的玩家加分详情

流程:
1. 确定鸟牌数量(rules.birdCount, 通常1-4)
2. 翻开最后N张牌墙作为鸟牌
3. 确定命中映射:
   - 中鸟加分规则(birdScoreRule):
     a. 固定加分(每人命中+N分)
     b. 翻倍(命中者的本局得分×2)
     c. 由输家承担/由庄家承担
4. 检查是否封顶(maxFan)
5. 写入结算明细
```

### 3.5 益阳歪胡子 (zipai_yiyang_waihuzi)

**核心**: 胡息计算系统 + 吃牌组合枚举

#### 胡息体系
```
胡息来源与分值:

来源        基础胡息  说明
─────────────────────────────────
吃(chi)      1息      每次吃牌获得
碰(peng)      2息      每次碰牌获得
偎(wei)       3息      偎牌获得
跑(pao)       6息      跑牌获得(较大)
提(ti)        9息      提牌获得(很大)
胡底息       6息      胡牌的基础息
特殊牌型加息   不定     大字/天胡/地胡/断夭等

HuxiAccumulator:
  totalHuxi = Σ(各次操作的胡息)
  needMinHuxi = rules.minHuxi (通常≥15息才可胡)

胡牌条件:
  totalHuxi ≥ needMinHuxi AND 手牌符合基本胡型
```

#### 吃牌组合枚举（最大难点）
```
问题: 歪胡子的吃牌规则灵活, 可能存在多种吃法

示例: 手牌 [小壹, 小贰, 叁, 肆, 小伍]
     上家出了 [肆]

可能的吃法:
  A. 吃 [贰,叁,肆] → 组合1 (贰+叁+上家的肆)
  B. 吃 [肆,伍]   → 等待后续还有[陆]来组成第二组
  C. 不吃, 过

ChiOption {
  combination: Tile[],       // 吃入的组合
  remainingAfterEat: Tile[],  // 吃后剩余的手牌
  huxiGained: number,        // 本次获得的胡息
  newTotalHuxi: number,      // 累计总胡息
  futurePotential: number     // 后续潜在胡息(评估值)
}

getChiOptions(hand, tilePlayed, rules) → ChiOption[]
  1. 枚举所有包含 tile 的连续3张组合
  2. 对每种组合, 计算胡息变化
  3. 过滤掉导致"死胡"(吃完后再也无法凑够胡息)的组合
  4. 按胡息收益排序返回
```

### 3.6 沅江千分 (poker_yuanjiang_qianfen)

**特色**: 叫分/抢庄 + 记分牌 + 升级/累计双模式

#### 叫分抢庄流程
```
CallBidPhase:
1. 发牌后每人查看手牌
2. 按座位顺序叫分(0/10/20/30... 最高上限)
3. 叫分最高者成为庄家(抢庄)
4. 如果无人叫分(都叫0) → 随机指定/轮庄
5. 庄家先出牌(或先扣底牌)

数据:
  CallBidResult {
    bids: Map<seat, number>,   // 每人的叫分
    bankerSeat: number,         // 庄家座位
    bottomScore: number         // 底分(从总积分中扣除)
  }
```

#### 计分牌机制
```
ScoringCards (可选配置):
  特定点数的牌在结算时有额外计分效果
  
配置示例:
  5 → 5分, 10 → 10分, K → 10分

结算时:
  每轮结束后, 统计各方获得的计分牌总数
  加入到本轮得分中
  影响升级判定(如果采用升级模式)

UpgradeMode (升级模式):
  - 目标分: 如1000分
  - 谁先到/超过目标分 → 升级胜利
  - 或者: 谁分数最低 → 淘汰
  - 升级后重新洗牌发牌, 等级提升

CumulativeMode (累计模式):
  - 多局累加, 最终总分最高者胜
  - 适合较长局数的玩法
```

---

## 四、回放数据结构

```typescript
interface ReplayData {
  meta: {
    replayId: string;
    roundId: bigint;
    roomId: bigint;
    roomNo: string;
    gameCode: string;           // mahjong_changsha
    ruleVersionSnapshot: any;    // 完整规则配置副本
    createdAt: ISODateString;
    durationSec: number;
    
    clientInfo: {
      versions: Map<seat, string>;  // 各玩家客户端版本
      deviceModels: Map<seat, string>;
      avgLatencyMs: number;
    };
  };
  
  dealInfo: {
    seedHash: string;
    dealerSeat: number;
    initialHands: Map<seat, Tile[]>;  // 所有玩家初始手牌(含服务端视角完整数据)
    remainingWall: Tile[];
  };
  
  actions: ReplayAction[];  // 完整操作流(见WS协议 action_result 格式)
  
  settlement: {
    endReason: string;
    playerResults: Array<{
      seat: number;
      userId: bigint;
      finalScore: number;
      detail: any;                // 游戏特有结算字段
    }>;
  };

  // 索引(用于快速查询)
  indexes: {
    byRoundId: 1;
    byRoomId: 1;
    byPlayer: "settlement.playerResults.userId";
    byDate: -1;
  };
}
```

---

## 五、测试策略

### 5.1 每款游戏必须通过的测试矩阵

| 测试项 | 方法 | 通过标准 |
|--------|------|---------|
| **洗牌随机性** | 10000局模拟 | 无重复局、卡牌分布均匀(卡方检验 p>0.05) |
| **操作合法性** | 单元测试(全排列) | 每种非法操作都被拒绝 |
| **胡牌判断正确性** | 穷举/验证 | 与人工/参考实现对比 100% 一致 |
| **结算一致性** | 10000局闭环 | 总积分变动=0(封闭系统) |
| **并发安全** | 多线程模拟 | 无死锁/竞态/数据不一致 |
| **异常恢复** | 断网注入 | 重连后状态与断前一致 |
| **边界情况** | 特殊牌型 | 天胡/地胡/十三幺/全不靠 等 |
| **性能** | Benchmark | 单局结算 < 50ms |

### 5.2 测试用例文件规范

```
games/{game_code}/tests/
├── test_shuffle.test.ts      // 洗牌测试
├── test_validate_action.test.ts // 操作校验
├── test_hu_check.test.ts      // 胡牌判断(含所有番型)
├── test_settlement.test.ts    // 结算算法
├── test_edge_cases.test.ts     // 边界情况
├── test_concurrent.test.ts    // 并发安全
├── test_recover.test.ts       // 异常恢复
├── fixtures/
│   ├── hands/                 // 预设手牌JSON
│   ├── scenarios/             // 预设场景
│   └── expected_results/     // 预期结果
└── performance/
    └── benchmark_settlement.ts
```
