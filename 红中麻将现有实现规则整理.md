# 红中麻将现有实现规则整理草案

> 本文用于后续规则确认、补充和功能优化。当前整理来源：
>
> 1. `Server/HongZhongMahjong/` 红中麻将服务端实现。
> 2. `Mahjong/` 通用麻将房间、玩家、规则、发牌、动作和结算实现。
> 3. `web_server` 红中麻将房间创建、表结构和规则配置透传逻辑。
> 4. `client_cocos` 红中麻将客户端现有口径。
> 5. `rules.md`、`DEV_PLAN.md`、`server_dev_plan.md` 等历史/规划文档。
>
> 标记说明：
>
> - `当前实现`：代码中已经实际执行的逻辑。
> - `配置字段`：代码会读取或保存该字段，但不一定真正生效。
> - `客户端口径`：当前 Cocos 客户端或规划文档表达的规则，不一定被服务端实现。
> - `待确认`：需要你补充定稿，后续再据此实现或修正。

---

## 1. 游戏定位

### 1.1 当前服务端定位

红中麻将当前在服务端作为一个独立游戏类型接入：

| 项目 | 当前实现 |
|---|---|
| 游戏类型 ID | `1032`，定义为 `GameType::HongZhongMahjong` |
| 服务端目录 | `Server/HongZhongMahjong/` |
| 房间类 | `HongZhongMahjongRoom : MahjongRoom` |
| 玩家类 | `HongZhongMahjongAvatar : MahjongAvatar` |
| 规则类 | 直接使用通用 `MahjongRule`，没有独立 `HongZhongMahjongRule` |
| 人数 | 4 人，继承 `MahjongRoom` 默认 `maxPlayerNums = 4` |
| 玩法说明注释 | 文件头写有“红中赖子玩法，红中作为万能牌” |
| 实际赖子实现 | 当前未看到红中万能牌替换算法，红中按普通字牌处理 |

服务端已经完成加载器、房间处理器、消息注册和基础牌局流程接入：

- `Server/main.cpp` 注册 `HongZhongMahjongLoader`。
- `Server/main.cpp` 创建 `HongZhongMahjongRoomHandler`。
- `Server/main.cpp` 注册 `HongZhongMahjongMessages`。
- `HongZhongMahjongRoomHandler` 绑定游戏类型 `1032`，并额外处理 `MsgHZSync`。

### 1.2 与产品/客户端口径的主要冲突

| 项目 | 客户端/规划口径 | 当前服务端实现 |
|---|---|---|
| 牌具 | Cocos 注释写“84 张，万条筒 + 红中 x4”，但该描述内部张数不一致 | 通用麻将牌池，默认 136 张，含筒条万、东南西北中发白 |
| 红中 | 红中可当任意牌，癞子/百搭 | 红中只是普通字牌，没有万能牌替换 |
| 吃牌 | 客户端写“红中不可吃”且覆写 `doActionChi` 禁吃 | 服务端默认 `allow_chi=false`；若配置打开，普通数牌可吃，红中作为字牌本身不可吃 |
| 番型 | 客户端列平胡、自摸、红中炮、全起人、海底捞、杠上开花、混一色 | 服务端按通用麻将番型：平胡、七对、豪七、碰碰胡、清一色、字一色、十三幺等 |
| 红中数量 | 规划文档提到 `hongzhong_count` | 服务端未读取该字段 |
| 红中赖子开关 | 规划文档提到 `hongzhong_as_laizi` | 服务端未读取该字段 |

结论：当前服务端更接近“通用 4 人麻将 + 红中麻将独立房间壳 + 红中麻将独立结算倍率”，不是完整的红中癞子玩法。

---

## 2. 牌具与座位

### 2.1 当前服务端牌具

`HongZhongMahjongRoom` 构造时创建的是 `std::make_shared<MahjongRule>()`，通用 `MahjongRule` 返回：

| 方法 | 返回值 | 含义 |
|---|---:|---|
| `hasFlower()` | `false` | 不带花牌 |
| `hasZiPai()` | `true` | 带字牌 |

因此 `MahjongDealer` 使用的总牌数为 136 张：

| 牌类 | 张数 | 当前是否包含 |
|---|---:|---|
| 筒 1-9 | 36 | 是 |
| 条 1-9 | 36 | 是 |
| 万 1-9 | 36 | 是 |
| 东南西北 | 16 | 是 |
| 中发白 | 12 | 是 |
| 花牌 | 0 | 否 |
| 合计 | 136 | 是 |

### 2.2 红中在当前牌具中的地位

| 项目 | 当前实现 |
|---|---|
| 红中枚举 | `MahjongTile::Pattern::Zhong` |
| 红中张数 | 4 张 |
| 是否参与普通胡牌 | 是，作为字牌参与 |
| 是否作为万能牌 | 否 |
| 是否可组成刻子/杠 | 是，与其他普通牌一样按数量判断 |
| 是否可组成顺子/吃 | 否，字牌不能参与顺子 |

### 2.3 座位与人数

| 项目 | 当前实现 |
|---|---|
| 玩家人数 | 4 人 |
| 座位编号 | `0, 1, 2, 3` |
| 开局条件 | 房间满员且所有玩家准备 |
| 首局默认庄家 | 继承基类 `_banker = 0` |

---

## 3. 房间与配置

### 3.1 房间加载

红中麻将房间由 `HongZhongMahjongLoader` 从数据库表 `game_hongzhong_mahjong` 读取：

| 字段 | 用途 |
|---|---|
| `venue_id` | 场地 ID，由加载器按 ID 查询 |
| `number` | 房号，同步给客户端 |
| `level` | 房间等级 |
| `rule_config` | 玩法配置 JSON，传给 `HongZhongMahjongRoom::parseRuleConfig` |

`web_server` 创建好友房时对红中麻将的 `rule_config` 只是原样保存请求 JSON；金币场 17-20 则只写入 `base_score`、`round_count`、`max_score` 等基础字段。

### 3.2 当前默认配置

`HongZhongMahjongRoom` 构造时默认值如下：

| 字段 | 默认值 | 当前是否生效 | 说明 |
|---|---:|---|---|
| `base_score` / `_diZhu` | `1` | 是 | 作为底分，结算金币 = 底分 x 分数单位 |
| `max_score` / `_maxScore` | `0` | 部分生效 | 大于 0 时封顶胡牌分，不封顶杠分 |
| `room_fee_type` | `0` | 未见生效 | 仅解析 |
| `round_count` | `8` | 未见生效 | 红中房间未按局数自动结束 |
| `allow_chi` | `false` | 是 | 映射到通用 `_chi` |
| `allow_peng` | `true` | 未见生效 | 解析但未拦截碰 |
| `allow_gang` | `true` | 未见生效 | 解析但未拦截杠 |
| `allow_zimo` | `true` | 未见生效 | 解析但未拦截自摸 |
| `allow_dianpao` | `true` | 是 | 映射到通用 `_dianPao` |
| `qidui_enabled` | `true` | 未见生效 | 通用规则始终检测七对 |
| `pengpenghu_enabled` | `true` | 未见生效 | 通用结算始终可能检测碰碰胡 |
| `zimo_double` | `true` | 未见生效 | 自摸当前固定翻 2 倍 |
| `dissolve_vote` | `true` | 是 | 控制是否允许解散投票 |
| `banker_rule` | `0` | 未见生效 | 解析但未覆写庄家规则 |

### 3.3 当前缺失的红中专属配置

历史规划文档提到过这些字段，但当前红中服务端未读取：

| 字段 | 规划含义 | 当前状态 |
|---|---|---|
| `hongzhong_count` | 红中数量 | 未读取 |
| `hongzhong_as_laizi` | 红中是否作为赖子 | 未读取 |
| `can_hu_with_hongzhong` | 是否允许红中参与胡牌 | 未读取 |
| `laizi_replace_rule` | 赖子替代规则 | 未读取 |
| `gang_score` | 杠分配置 | 未读取，杠分固定 |
| `hongzhong_pao` | 红中炮番型 | 未实现 |
| `hunyise` / `hun_yi_se` | 混一色 | 未实现，当前只有清一色、字一色 |

---

## 4. 进入、准备、开始

### 4.1 进入房间

| 场景 | 当前实现 |
|---|---|
| 牌局未开始 | 允许进入 |
| 牌局进行中 | 拒绝进入，提示“游戏正在进行中，不能进入房间” |
| 机器人 | 走同一套进入检查 |

金币/押金检查由 `GameRoom` 基类处理。红中麻将设置押金：

```text
cash_pledge = base_score * 50
```

### 4.2 离开房间

| 场景 | 当前实现 |
|---|---|
| 牌局未开始 | 允许离开 |
| 牌局进行中 | 拒绝离开，返回状态码 `1` |
| 房间无人 | 调用 `gameOver()` |

### 4.3 准备与开局

玩家发送通用 `MsgPlayerReady` 后：

1. 房间必须处于 `_roundState == NotStarted`。
2. 服务端把该玩家标记为 ready。
3. 广播 `MsgPlayerReadyResp`。
4. 当 `isFull() && isAllReady()` 时调用 `startRound()`。

### 4.4 开局流程

`startRound()` 当前流程：

1. `_roundState = Underway`。
2. 备份当前庄家到 `_backupBanker`。
3. 调用 `clean()` 清理上一局手牌、动作、分数、踢人标记等。
4. 向每位玩家发送 `MsgHZStartRound`，携带庄家座位。
5. 调用通用 `dealTiles()` 发牌。
6. 若 `_roundNo == 0`，从 `game_hongzhong_mahjong_record` 查询历史最大局号。
7. `_roundNo++`。

---

## 5. 发牌、摸牌、出牌

### 5.1 发牌

通用 `MahjongRoom::dealTiles()` 执行：

| 步骤 | 当前实现 |
|---|---|
| 洗牌 | `MahjongDealer::shuffle()` 随机打乱牌池 |
| 初始牌 | 每位玩家先发 13 张 |
| 排序 | 每人手牌排序 |
| 备份 | 保存初始手牌用于回放 |
| 听牌检测 | 对每人的 13 张牌调用 `checkTingPai()` |
| 发牌消息 | 每位玩家只收到自己的手牌 |
| 庄家首摸 | 发完后当前活动玩家切为庄家，庄家摸第一张 |

产品描述可写成“庄家起手 14 张，闲家起手 13 张”。代码实现是“先全员 13 张，再庄家摸一张”，效果等价。

### 5.2 摸牌

`fetchTile()` 当前逻辑：

1. 如果牌池已经摸完，进入流局处理。
2. 从牌墙前端摸牌；杠后补牌时从牌墙后端摸牌。
3. 先检查摸到的牌是否可胡，若可胡则生成自摸动作选项。
4. 把牌插入玩家手牌，记录本次摸牌 ID。
5. 广播/通知摸牌消息：本人收到具体牌，其他玩家只知道谁摸牌和剩余张数。
6. 检查加杠、暗杠动作。
7. 若有自摸/杠选项，同时生成出牌选项；否则直接进入等待出牌状态。

`allow_zimo=false` 当前不会阻止第 3 步生成自摸选项。

### 5.3 出牌

玩家出牌时：

1. 必须是当前活动玩家。
2. 必须处于等待出牌状态，或处于动作等待状态且自己拥有出牌选项。
3. 从手牌移除指定 ID 的牌。
4. 记录到已出牌列表。
5. 更新出牌玩家听牌信息。
6. 广播 `MsgPlayTile`。
7. 为其他玩家按顺序生成点炮、直杠、碰、吃动作选项。
8. 若无人可操作，切换到下家摸牌。

---

## 6. 吃、碰、杠

### 6.1 动作优先级

通用麻将动作执行优先级固定为：

```text
胡 > 杠 > 碰 > 吃
```

也就是说：

- 有人可胡时，杠/碰/吃必须等待胡牌选择结束。
- 有人可杠时，碰/吃必须等待杠选择结束。
- 有人可碰时，吃必须等待碰选择结束。

### 6.2 吃牌

| 项目 | 当前实现 |
|---|---|
| 默认是否允许 | 不允许，`allow_chi=false` |
| 配置打开后 | 只有出牌者下家可以吃 |
| 可吃牌型 | 只能用同花色数牌组成连续顺子 |
| 字牌/红中 | 不能吃 |

注意：客户端红中房间覆写 `doActionChi()` 直接禁吃，但服务端若收到 `allow_chi=true` 的配置，仍会对普通数牌生成吃选项。

### 6.3 碰牌

| 项目 | 当前实现 |
|---|---|
| 触发条件 | 其他玩家打出一张牌，自己手里有 2 张同牌 |
| 过碰限制 | 过碰某张牌后，下次摸牌前不能再碰同一张 |
| 配置开关 | `allow_peng` 已解析但未用于拦截 |
| 红中 | 可按普通字牌碰 |

碰牌后：

1. 从手牌移除 2 张同牌。
2. 与被碰的牌组成一个 `Peng` 章。
3. 当前活动玩家切换为碰牌者。
4. 碰牌者进入出牌/杠选择流程。

### 6.4 直杠

| 项目 | 当前实现 |
|---|---|
| 触发条件 | 其他玩家打出一张牌，自己手里有 3 张同牌 |
| 配置开关 | `allow_gang` 已解析但未用于拦截 |
| 得分 | 杠牌者从放杠者处得 1 分单位 |
| 补牌 | 直杠后从牌墙后端补牌 |
| 红中 | 可按普通字牌直杠 |

### 6.5 加杠

| 项目 | 当前实现 |
|---|---|
| 触发条件 | 自己已有碰章，手里又有同牌 |
| 延迟加杠 | `_delayJiaGang=true`，不是刚摸到的牌也可以加杠 |
| 得分 | 杠牌者从其他 3 家各得 1 分单位 |
| 抢杠 | 允许点炮时，其他玩家可抢加杠 |
| 红中 | 可按普通字牌加杠 |

加杠时，服务端会先判断其他玩家是否能抢杠。若有人可胡：

1. 先把加杠动作记录为 `JiaGang`。
2. 给可胡玩家生成 `DianPao` 动作选项。
3. 若所有可胡玩家都放弃，当前玩家完成加杠并补牌。
4. 若有人抢杠，放炮玩家的加杠章会回退为碰章。

### 6.6 暗杠

| 项目 | 当前实现 |
|---|---|
| 触发条件 | 自己手里有 4 张同牌 |
| 配置开关 | `allow_gang` 已解析但未用于拦截 |
| 得分 | 杠牌者从其他 3 家各得 2 分单位 |
| 补牌 | 暗杠后从牌墙后端补牌 |
| 是否可见 | 红中房间设置 `_anGangVisible=true`，其他玩家可见暗杠牌 |
| 红中 | 可按普通字牌暗杠 |

---

## 7. 听牌与胡牌

### 7.1 听牌缓存

当前能否胡牌依赖 `_tingTiles` 听牌缓存：

| 更新时机 | 当前实现 |
|---|---|
| 发完 13 张牌后 | 更新 |
| 玩家出牌后 | 更新出牌玩家 |
| 玩家杠牌后 | 更新杠牌玩家 |
| 吃/碰后 | 未直接调用 `checkTingPai()`，后续出牌后会更新 |

`canHu(mt)` 只是检查目标牌是否存在于 `_tingTiles`，不是实时完整重算。

### 7.2 当前支持的基础胡牌结构

当前红中麻将使用通用 `MahjongRule`，支持：

| 胡牌结构 | 当前实现 |
|---|---|
| 平胡 | 支持，普通 4 组面子 + 1 对将 |
| 七小对 | 支持 |
| 豪华七小对 | 支持，按 4 张相同牌数量区分单豪/双豪/三豪 |
| 碰碰胡 | 支持 |
| 清一色 | 支持，作为附加样式翻倍 |
| 字一色 | 支持，作为附加样式翻倍 |
| 十三幺 | 有限支持：当前听牌检测只覆盖 13 张幺九字牌齐全的十三面听，不是完整十三幺听牌算法 |
| 十三烂 | 通用规则有代码，但默认 `_13Lan=false`，红中未开启 |
| 扩展胡牌 | `checkHuStyleEx()` 默认返回 false，红中无扩展 |

### 7.3 当前平胡规则

| 项目 | 当前实现 |
|---|---|
| 将牌限制 | 无，任意牌可作将 |
| 数牌顺子 | 支持 |
| 数牌刻子 | 支持 |
| 字牌刻子 | 支持 |
| 字牌顺子 | 不支持 |
| 花牌 | 无花牌 |

### 7.4 单吊、边张、卡张

当平胡只听一张牌时，通用规则会尝试标记：

| 样式 | 当前实现 |
|---|---|
| 单吊 | 支持 |
| 边张 | 支持 |
| 卡张 | 支持 |

这些样式在红中结算中基础分为 2。

### 7.5 七小对与豪华七小对

当前检测逻辑：

1. 必须没有任何吃、碰、杠章。
2. 14 张牌每两张相同。
3. 若存在 4 张相同牌，则计入豪华七小对数量。

当前计分：

| 样式 | 基础分 |
|---|---:|
| 七小对 | 4 |
| 单豪华七小对 | 8 |
| 双豪华七小对 | 12 |
| 三豪华七小对 | 16 |

`qidui_enabled=false` 当前不会关闭七小对检测。

### 7.6 碰碰胡

当前检测逻辑：

- 手牌和章合并后，不能出现单张。
- 只能有一组对子作为将。
- 其他均为刻子/杠。

当前基础分为 2。

`pengpenghu_enabled=false` 当前不会关闭碰碰胡检测。

### 7.7 红中赖子

这是当前实现与玩法名称差异最大的部分。

| 项目 | 当前服务端实现 |
|---|---|
| 红中作为万能牌替代任意牌 | 未实现 |
| 多红中最优替代组合 | 未实现 |
| 红中参与七对补对子 | 未实现 |
| 红中补顺子/刻子/将 | 未实现 |
| 红中替代路径记录 | 未实现 |
| 红中数量展示/结算 | 未实现 |
| 红中作为普通“中”胡牌 | 支持 |

也就是说，当前如果玩家手里有红中，服务端只会把它看作普通字牌“中”，不会把它当任意牌。

---

## 8. 胡牌方式

### 8.1 自摸

当前逻辑：

1. 玩家摸牌后，如果摸到的牌在听牌缓存里，则生成 `ZiMo` 动作选项。
2. 玩家选择自摸后，调用 `detectHuStyle(true, mt)`。
3. 胡牌方式加入 `ZiMo`。
4. 其他 3 家各赔一份胡牌分。

当前 `allow_zimo=false` 不会禁止自摸。

当前 `zimo_double=false` 不会取消自摸翻倍，因为 `HongZhongMahjongAvatar::calcHuScore()` 固定对自摸乘 2。

### 8.2 点炮

当前逻辑：

1. 玩家出牌后，其他玩家如果 `canHu(出牌)` 且允许点炮，会生成 `DianPao` 动作。
2. 玩家选择点炮后，调用 `detectHuStyle(false, mt)`。
3. 胡牌方式加入 `DianPao`。
4. 放炮者一人承担胡牌分。

`allow_dianpao=false` 会关闭点炮，也会间接关闭抢杠胡。

### 8.3 一炮多响

通用麻将默认：

| 开关 | 当前值 | 效果 |
|---|---:|---|
| `_mutiDianPao` | `true` | 允许一炮多响 |
| `_allDianPao` | `true` | 只要一家选择胡，所有仍可胡且未过胡的玩家都会一起胡 |

红中房间没有覆写这两个值。

### 8.4 过胡

当前逻辑：

- 玩家放弃点炮动作后，记录到 `_passedHu`。
- 在该玩家下次摸牌之前，不能再点炮胡。
- 当前实现只要 `_passedHu` 非空，就禁止点炮，不区分是否同一张牌。
- 玩家自己出牌时会清空过胡记录。

### 8.5 抢杠胡

当前逻辑：

1. 只针对加杠产生抢杠。
2. 需要 `allow_dianpao=true`。
3. 其他玩家可胡该加杠牌时，生成 `DianPao` 动作。
4. 有人胡则加杠者为放炮者。
5. 加杠章回退为碰章。
6. 胡牌方式加入 `QiangGangHu1/2/3/4`。

当前计分只对 `QiangGangHu1` 乘 2，二连/三连/四连抢杠胡没有额外倍率处理。

### 8.6 杠上花

当前逻辑：

- 玩家杠后从牌墙后端补牌。
- 若补牌后自摸，则根据本轮连续杠数量加入 `GangShangHua1/2/3/4`。
- 当前计分只对 `GangShangHua1` 乘 2。

### 8.7 杠上炮

当前逻辑：

- 玩家杠后补牌，再打出牌让别人胡，加入 `GangShangPao1/2/3/4`。
- 当前计分只对 `GangShangPao1` 乘 2。
- `_gangShangPaoVetoed=false`，杠上炮不会否决放炮者最近的杠分。

### 8.8 天胡、地胡、人胡

当前通用 `doHu()` 会识别：

| 胡牌方式 | 当前触发 |
|---|---|
| 天胡 | 庄家第一张摸牌自摸，且桌面无吃碰杠 |
| 地胡 | 庄家第一张出牌放炮，且胡家尚未出过牌、桌面无吃碰杠 |
| 人胡 | 闲家第一圈内自摸或放炮，且桌面无吃碰杠 |

当前红中计分：

| 胡牌方式 | 当前倍率 |
|---|---:|
| 天胡 | x4 |
| 地胡 | x4 |
| 人胡 | 识别但未额外计分 |

### 8.9 海底

当前通用逻辑在牌池剩余达到 `_tilesLeft` 时识别海底：

| 胡牌方式 | 当前触发 | 当前倍率 |
|---|---|---:|
| 海底捞月 | 最后一张自摸 | x2 |
| 海底炮 | 最后一张打出后点炮 | x2 |

红中房间 `_tilesLeft` 默认 0，等牌池摸空前最后一张牌触发。

---

## 9. 当前计分规则

### 9.1 胡牌基础分

`HongZhongMahjongAvatar::calcHuScore()` 当前先按胡牌样式取一个基础分：

| 胡牌样式 | 基础分 |
|---|---:|
| 平胡 | 1 |
| 单吊 | 2 |
| 边张 | 2 |
| 卡张 | 2 |
| 碰碰胡 | 2 |
| 七小对 | 4 |
| 单豪华七小对 | 8 |
| 双豪华七小对 | 12 |
| 三豪华七小对 | 16 |
| 十三幺 | 8 |

选择顺序是代码中的 `if / else if`，不是所有基础样式叠加。清一色、字一色、门清、自摸等作为后续乘法叠加。

### 9.2 附加倍率

当前红中麻将附加倍率：

| 附加项 | 倍率 | 说明 |
|---|---:|---|
| 清一色 | x2 | 所有手牌和章为同一花色 |
| 字一色 | x4 | 全部为字牌 |
| 门清 | x2 | 无吃、碰、直杠、加杠；暗杠仍算门清 |
| 自摸 | x2 | 固定生效，不受 `zimo_double` 控制 |
| 杠上花1 | x2 | 只检查 `GangShangHua1` |
| 杠上炮1 | x2 | 只检查 `GangShangPao1` |
| 抢杠胡1 | x2 | 只检查 `QiangGangHu1` |
| 海底捞月 | x2 | |
| 海底炮 | x2 | |
| 天胡 | x4 | |
| 地胡 | x4 | |

已识别但当前未额外计分的方式：

- 人胡
- 全求人
- 全求炮
- 明杠标记
- 二连/三连/四连杠上花
- 二连/三连/四连杠上炮
- 二连/三连/四连抢杠胡

### 9.3 胡牌封顶

`max_score` 当前只在胡牌分计算后进行封顶：

```text
score = avatar.calcHuScore()
if max_score > 0 && score > max_score:
    score = max_score
```

注意：

- 封顶发生在单个胡牌者的胡牌分单位上。
- 杠分不受该封顶限制。
- 封顶后再按点炮/自摸分摊。

### 9.4 杠分

| 杠类型 | 当前分数单位 | 支付方 |
|---|---:|---|
| 直杠 | 1 | 放杠者支付 |
| 加杠 | 每家 1 | 其他三家各支付 |
| 暗杠 | 每家 2 | 其他三家各支付 |

### 9.5 分数单位与金币

当前结算分两层：

1. `score`：玩法分数单位，用于结算数据和战绩。
2. `winGold`：真实金币变化，经过底分和押金清算后得到。

金币换算基础：

```text
原始金币债务 = base_score * 分数单位
```

然后通过 `DebtLiquidation` 按玩家当前押金做清算，避免清算后押金为负。

### 9.6 自摸与点炮分摊

| 胡牌方式 | 当前分摊 |
|---|---|
| 自摸 | 其他 3 家各赔 `score` |
| 点炮 | 放炮者一人赔 `score` |
| 一炮多响 | 放炮者分别赔给每个胡牌者 |

---

## 10. 流局与下一局

### 10.1 流局

当前流局触发：

- `fetchTile()` 前检查 `earlyTermination()`。
- 当牌池剩余数小于等于 `_tilesLeft` 时流局。
- 红中房间 `_tilesLeft` 默认 0，因此基本是摸完牌池后流局。

流局处理：

1. 状态进入 `End`。
2. `calcHuScore()` 因 `_hu=false` 不计算胡分。
3. 调用 `doJieSuan()`，结算数据中 `hu=0`，分数和金币通常为 0。
4. 调用 `afterHu()` 保存记录、重置 ready 或踢出金币不足玩家。
5. 下局庄家设置为最后一个摸牌玩家。

### 10.2 下一局庄家

通用 `bankerNextRound()` 当前规则：

| 场景 | 下局庄家 |
|---|---|
| 单人胡牌 | 胡牌者坐庄 |
| 一炮多响 | 放炮者坐庄 |
| 流局 | 最后摸牌者坐庄 |

`banker_rule` 配置当前未参与逻辑。

### 10.3 局数结束

`round_count` 已解析，但红中房间当前没有类似“达到局数后解散/结束房间”的逻辑。

对比：桃江麻将实现中已有 `_roundCount` 结束判断；红中麻将尚未接入。

---

## 11. 解散投票

### 11.1 发起解散

当前实现：

| 条件 | 当前实现 |
|---|---|
| `dissolve_vote=false` | 禁止发起 |
| 已有投票进行中 | 忽略新的发起 |
| 发起时机 | 未限制必须牌局进行中，未开始也可发起 |
| 发起人选择 | 自动记为同意 |

### 11.2 投票选项

| choice | 含义 |
|---:|---|
| `1` | 同意 |
| `2` | 拒绝 |

### 11.3 通过和否决

4 人房当前规则：

| 场景 | 当前实现 |
|---|---|
| 同意人数 > 2 | 解散房间 |
| 拒绝人数 >= 2 | 解散投票作废 |
| 300 秒超时 | 未选择玩家自动同意 |

解散成功后：

1. 广播 `MsgDisband`。
2. `_roundState = NotStarted`。
3. 踢出全部玩家。
4. 调用 `gameOver()`。

---

## 12. 协议消息

### 12.1 红中麻将专属消息

| 消息 | 方向 | 用途 |
|---|---|---|
| `MsgHZSync` | 客户端 -> 服务端 | 同步红中麻将房间数据 |
| `MsgHZSyncResp` | 服务端 -> 客户端 | 返回房间号、资产、底分、手牌、出牌、章、状态等 |
| `MsgHZStartRound` | 服务端 -> 客户端 | 通知新一局开始，携带庄家 |
| `MsgHZSettlement` | 服务端 -> 客户端 | 单局结算，携带 `MahjongSettlement`、金币、是否踢人 |
| `MsgHZDisbandVote` | 服务端 -> 客户端 | 通知解散投票状态 |

### 12.2 `MsgHZSyncResp` 字段

| 字段 | 含义 |
|---|---|
| `number` | 房号 |
| `gold` | 玩家账户金币 |
| `diamond` | 玩家钻石 |
| `diZhu` | 底分 |
| `chi` | 是否允许吃 |
| `dianPao` | 是否允许点炮 |
| `hasFetch` | 当前玩家是否有摸起牌 |
| `seat` | 当前玩家座位 |
| `roundState` | 牌局状态 |
| `disbandState` | 解散状态 |
| `banker` | 当前庄家 |
| `leftTiles` | 剩余牌数 |
| `handTileNums[4]` | 各座位手牌数量 |
| `fetchTile` | 当前玩家摸起牌 |
| `handTiles` | 当前玩家手牌 |
| `playedTiles[4]` | 各玩家未被吃碰杠拿走的出牌 |
| `chapters[4]` | 各玩家吃碰杠章 |

### 12.3 共用麻将消息

红中麻将还复用通用麻将消息，包括：

- `MsgMahjongTiles`：发牌。
- `MsgFetchTile`：摸牌。
- `MsgActionOption`：动作选项。
- `MsgDoActionOption`：执行动作。
- `MsgPassActionOption`：过。
- `MsgWaitAction`：等待动作状态。
- `MsgActionOptionFinish`：动作选项结束。
- `MsgPlayTile`：出牌。
- `MsgGangTile`：杠牌。
- `MsgPengChiTile`：碰/吃。
- `MsgTingTile`：听牌提示。
- `MsgHuTile`：胡牌通知。
- `MsgShowTiles`：摊牌。
- `MsgPassTip`：过胡/过碰提示。

### 12.4 客户端协议差异

当前 Cocos 红中麻将类中存在以下差异：

| 项目 | Cocos 当前写法 | 服务端当前写法 |
|---|---|---|
| 消息前缀 | `MsgHongzhongMahjong` | `MsgHZ...` |
| 红中识别 | `pattern >= 4 && number === 5` | 服务端红中为 `pattern = 8, number = 0` |
| 牌具注释 | 写“84 张”，但同时又写“万条筒各 1-9 + 红中 x4”，两者张数不一致 | 136 张 |
| 吃牌 | 客户端强制不吃 | 服务端配置可打开普通吃 |

后续客户端对接需要按最终服务端协议统一。

---

## 13. 回放与战绩

### 13.1 当前回放数据

`HongZhongMahjongPlaybackData` 保存：

| 字段 | 含义 |
|---|---|
| `dealedTiles[4]` | 初始发牌 |
| `chapters[4]` | 各玩家章 |
| `actions` | 牌局动作序列 |
| `actors` | 活动玩家序列 |
| `winGolds[4]` | 本局金币输赢 |
| `settlement` | 单局结算数据 |
| `randomSeedHash` | 随机种子 hash |

保存时会：

1. 使用 MessagePack 打包。
2. 使用 zlib 压缩。
3. 使用 Base64 保存到数据库。

### 13.2 当前记录任务

`HongZhongMahjongRecordTask` 写入：

```text
game_hongzhong_mahjong_record
```

字段包括：

- `venue_id`
- `round_no`
- `banker`
- `player_id0..3`
- `score0..3`
- `wingold0..3`
- `random_seed_hash`
- `playback`

### 13.3 数据落库风险

当前发现两个需要优先确认的工程风险：

| 风险 | 说明 |
|---|---|
| 红中记录表缺失 | 现有 `v4_add_record_tables.sql` 只创建了 `game_taojiang_mahjong_record`，未看到 `game_hongzhong_mahjong_record` 建表 |
| 字段命名不一致 | C++ 记录任务写 `wingold0`，现有桃江 SQL 使用 `win_gold0` |

如果数据库中没有手工建过红中记录表，红中结算后的回放/战绩保存会失败。

---

## 14. 当前已实现能力清单

### 14.1 牌局流程

| 能力 | 当前状态 |
|---|---|
| 4 人房间 | 已实现 |
| 房间加载 | 已实现 |
| 准备开局 | 已实现 |
| 发牌 | 已实现 |
| 摸牌 | 已实现 |
| 出牌 | 已实现 |
| 吃/碰/杠/胡通用动作 | 已实现 |
| 过胡/过碰限制 | 已实现 |
| 听牌提示 | 已实现 |
| 流局 | 已实现 |
| 单局结算 | 已实现 |
| 回放保存 | 代码已实现，但依赖表结构 |
| 解散投票 | 已实现 |

### 14.2 红中麻将专属能力

| 能力 | 当前状态 |
|---|---|
| 独立游戏类型 `1032` | 已实现 |
| 独立房间表 | 已实现 |
| 独立同步消息 `MsgHZSync` | 已实现 |
| 独立开局消息 `MsgHZStartRound` | 已实现 |
| 独立结算消息 `MsgHZSettlement` | 已实现 |
| 红中专属计分类 | 部分实现，主要是倍率表 |
| 红中赖子胡牌 | 未实现 |
| 红中数量/替代路径 | 未实现 |
| 红中炮/混一色等地方番型 | 未实现 |

---

## 15. 当前规则缺口与待确认

### 15.1 牌具需要定稿

待确认：

1. 最终牌具是否为 84 张、112 张，还是 136 张？如果是“筒条万 1-9 各 4 张 + 红中 4 张”，按常规计算应为 112 张。
2. 是否保留东南西北、发、白？
3. 是否只有红中这一种字牌？
4. 红中数量是否固定 4 张，还是配置 `hongzhong_count`？
5. 是否允许无字牌的 108 张 + 红中扩展玩法？

### 15.2 红中赖子规则需要定稿

待确认：

1. 红中是否永远作为万能牌？
2. 红中是否也可以当本身“中”使用？
3. 红中能否被打出？
4. 红中能否被碰、明杠、暗杠、加杠？
5. 红中是否能参与七对、豪华七对？
6. 多个红中替代时，是否按最大番型最优解结算？
7. 胡牌时是否需要记录红中替代路径给回放/审计？
8. 是否存在“红中炮”“红中作将”“无红中硬胡”等特殊番型？

### 15.3 动作规则需要定稿

待确认：

1. 红中麻将是否完全禁止吃？
2. 若允许吃，是否只允许数牌吃？
3. 是否允许一炮多响？
4. 是否“一家点胡则全部可胡者自动胡”？
5. 过胡是只过当前牌，还是过所有点炮直到下次摸牌？
6. 过碰是否只限制同一张牌？
7. 加杠是否允许延迟加杠？
8. 暗杠是否对其他玩家可见？

### 15.4 胡牌番型需要定稿

当前服务端已有但需确认是否属于红中麻将：

- 平胡
- 单吊
- 边张
- 卡张
- 碰碰胡
- 七小对
- 豪华七小对
- 清一色
- 字一色
- 十三幺
- 门清
- 天胡
- 地胡
- 人胡
- 海底捞月
- 海底炮
- 杠上花
- 杠上炮
- 抢杠胡

客户端/规划提到但服务端未实现或名称不同：

- 红中炮
- 全起人/全求人
- 混一色
- 红中赖子番型
- 无红中/硬胡类番型

### 15.5 计分规则需要定稿

待确认：

1. 当前基础分表是否符合目标红中麻将？
2. 自摸是否固定翻倍，还是可配置？
3. 杠上花、杠上炮、抢杠胡是否只翻 1 次，还是按连续杠递增？
4. 人胡、全求人、全求炮是否需要计分？
5. 杠分是否固定：直杠 1、加杠每家 1、暗杠每家 2？
6. `max_score` 封顶的是胡牌番数、分数单位、还是金币输赢？
7. 红中赖子最优解是否按“最高分”选择？
8. 一炮多响时封顶和赔付如何计算？

### 15.6 房间生命周期需要定稿

待确认：

1. `round_count` 达到后是否自动结束房间？
2. 好友房、练习房、金币场是否使用同一规则？
3. `room_fee_type` 如何扣费？
4. 局数结束后是否踢出所有玩家并 `gameOver()`？
5. 中途解散是否保存当前局记录？
6. 未开局解散是否删除场地数据库记录？

### 15.7 数据库与协议需要补齐

待确认/待实现：

1. 创建 `game_hongzhong_mahjong_record` 表。
2. 统一 `win_gold0` 与 `wingold0` 字段命名。
3. 后台查询红中战绩/回放接口。
4. 客户端消息名与服务端 `MsgHZ...` 对齐。
5. 客户端红中识别条件修正为 `pattern == 8`。
6. 同步消息是否需要下发红中规则配置、红中数量、赖子状态。

---

## 16. 建议后续实现顺序

### 16.1 先定规则

建议先由你补全并定稿以下 5 个核心规则：

1. 牌具：84 张、112 张还是 136 张。
2. 红中：是否万能、能否打出/碰杠、是否可当本身。
3. 胡牌：支持哪些番型，哪些可叠加。
4. 计分：基础分、倍率、封顶、自摸/点炮/杠分。
5. 房间：局数结束、解散、金币场/好友房差异。

### 16.2 再补服务端核心

建议服务端优先级：

1. 新增 `HongZhongMahjongRule`，不要继续直接使用通用 `MahjongRule`。
2. 改造牌池，按定稿牌具生成。
3. 实现红中赖子听牌/胡牌算法。
4. 实现红中赖子番型最优解。
5. 让 `allow_peng/allow_gang/allow_zimo/qidui_enabled/pengpenghu_enabled/zimo_double` 真正生效。
6. 补齐 `round_count` 结束逻辑。
7. 补齐红中记录表和回放字段。

### 16.3 最后对齐客户端和后台

建议客户端/后台优先级：

1. 客户端协议名对齐 `MsgHZ...` 或服务端统一改名。
2. 客户端红中识别修正。
3. 同步并展示最终规则配置。
4. 结算页展示红中替代、番型、倍率、杠分。
5. 后台规则配置页补齐红中专属字段。
6. 回放页展示红中替代路径。

---

## 17. 代码索引

| 模块 | 文件 |
|---|---|
| 游戏类型 | `Server/GameDefines.h` |
| 服务启动注册 | `Server/main.cpp` |
| 红中房间 | `Server/HongZhongMahjong/HongZhongMahjongRoom.h/.cpp` |
| 红中玩家 | `Server/HongZhongMahjong/HongZhongMahjongAvatar.h/.cpp` |
| 红中消息 | `Server/HongZhongMahjong/HongZhongMahjongMessages.h/.cpp` |
| 红中加载器 | `Server/HongZhongMahjong/HongZhongMahjongLoader.h/.cpp` |
| 红中房间处理器 | `Server/HongZhongMahjong/HongZhongMahjongRoomHandler.h/.cpp` |
| 红中记录任务 | `Server/HongZhongMahjong/HongZhongMahjongRecordTask.h/.cpp` |
| 红中回放 | `Server/HongZhongMahjong/HongZhongMahjongPlayback.h/.cpp` |
| 通用麻将房间 | `Mahjong/MahjongRoom.h/.cpp` |
| 通用麻将玩家 | `Mahjong/MahjongAvatar.h/.cpp` |
| 通用麻将规则 | `Mahjong/MahjongRule.h/.cpp` |
| 通用麻将牌 | `Mahjong/MahjongTile.h/.cpp` |
| 通用麻将发牌器 | `Mahjong/MahjongDealer.h/.cpp` |
| 通用麻将结算数据 | `Mahjong/MahjongSettlement.h/.cpp` |
| Web 房间实体 | `web_server/web-server/niuma-admin/src/main/java/com/niuma/admin/entity/GameHongzhongMahjong.java` |
| Web 房间创建 | `web_server/web-server/niuma-admin/src/main/java/com/niuma/admin/service/impl/GameServiceImpl.java` |
| Cocos 红中房间 | `client_cocos/client-cocos/assets/Scripts/Games/HongzhongMahjong/HongzhongMahjongRoom.ts` |
