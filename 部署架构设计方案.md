# H. 部署架构设计方案

> 版本：v1.0 | Kubernetes + Docker + CI/CD | 四环境部署

---

## 一、部署环境规划

### 1.1 四套环境定义

| 环境 | 用途 | 域名示例 | 数据 | 规模 |
|------|------|---------|------|------|
| **DEV** (开发) | 开发者日常调试 | dev-api.example.com | 测试种子数据(可随意重置) | 最小配置(1副本) |
| **TEST** (测试) | QA功能/性能测试 | test-api.example.com | 接近生产的模拟数据 | 标准配置(2-3副本) |
| **STAGING** (预发) | 上线前最终验证/UAT | staging-api.example.com | 生产数据镜像(只读同步) | 同生产配置 |
| **PROD** (生产) | 正式线上运营 | api.example.com | 真实业务数据 | 高可用配置 |

### 1.2 环境隔离策略

```
┌───────────────────────────────────────────────────────┐
│                    PROD (生产)                         │
│  VPC: 10.0.0.0/16                                    │
│  ┌─────────┐ ┌─────────┐ ┌──────────┐ ┌───────────┐ │
│  │ K8s主集群│ │ RDS主库 │ │ Redis集群│ │ OSS/OBS   │ │
│  │ 3master │ │ 1主2从  │ │ 3shard   │ │ 对象存储  │ │
│  └─────────┘ └─────────┘ └──────────┘ └───────────┘ │
├───────────────────────────────────────────────────────┤
│                   STAGING (预发)                       │
│  VPC: 10.1.0.0/16                                    │
│  ┌─────────┐ ┌─────────┐ ┌──────────┐               │
│  │ K8s单master│ │ RDS只读实例 │ │ Redis单机 │               │
│  └─────────┘ └─────────┘ └──────────┘               │
├───────────────────────────────────────────────────────┤
│                    TEST (测试)                         │
│  VPC: 10.2.0.0/16                                    │
│  ┌─────────┐ ┌─────────┐ ┌──────────┐               │
│  │ K8s单节点│ │ RDS独立 │ │ Redis单机 │               │
│  └─────────┘ └─────────┘ └──────────┘               │
├───────────────────────────────────────────────────────┤
│                     DEV (开发)                         │
│  VPC: 10.3.0.0/16 或 本地Docker                       │
│  ┌─────────┐ ┌─────────┐                              │
│  │ Docker-Com│ │ SQLite/  │                              │
│  │ pose     │ │ 内存Redis│                              │
│  └─────────┘ └─────────┘                              │
└───────────────────────────────────────────────────────┘

网络规则:
- DEV/TEST/STAGING ↔ PROD: 完全隔离, 无直接网络通路
- STAGING → PROD DB: 只读复制通道(用于数据校验)
- 所有环境 → 公网: 仅通过API Gateway暴露必要端口
```

---

## 二、Kubernetes 集群设计

### 2.1 生产集群拓扑

```
                          ┌─────────────────────┐
                          │    DNS / CDN 层       │
                          │  Cloudflare/AliDNS    │
                          │  HTTPS终止 + DDoS防护  │
                          └──────────┬──────────┘
                                     │
                          ┌──────────▼──────────┐
                          │   API Gateway (Ingress)│
                          │   Nginx Ingress        │
                          │   Cert-Manager(TLS)    │
                          │   Rate Limiting         │
                          └──────────┬──────────┘
                                     │
              ┌──────────────────────┼──────────────────────┐
              │                      │                      │
   ┌──────────▼──────┐    ┌─────────▼────────┐   ┌────────▼────────┐
   │   Namespace:     │    │   Namespace:      │   │   Namespace:     │
   │   gateway-system │    │   game-services   │   │   infra-system   │
   ├──────────────────┤    ├──────────────────┤   ├─────────────────┤
   │ ┌──────────────┐ │    │ ┌──────────────┐ │   │ ┌─────────────┐  │
   │ │api-gateway   │ │    │ │room-service  │ │   │ │postgresql   │  │
   │ │(3 replicas)  │ │    │ │(3 replicas)  │ │   │ │(StatefulSet)│  │
   │ └──────────────┘ │    │ └──────────────┘ │   │ ├─────────────┤  │
   │ ┌──────────────┐ │    │ ┌──────────────┐ │   │ │redis-cluster│  │
   │ │ws-gateway    │ │    │ │game-engine   │ │   │ │(6 nodes)    │  │
   │ │(3 replicas)  │ │    │ │(per-game pod)│ │   │ └─────────────┘  │
   │ └──────────────┘ │    │ └──────────────┘ │   │ ┌─────────────┐  │
   └──────────────────┘    │ ...17 services   │   │ │rabbitmq/pulsar│  │
                            └──────────────────┘   │ │(消息队列)     │  │
                                                    │ └─────────────┘  │
                                                    │ ┌─────────────┐  │
                                                    │ │minio/oss     │  │
                                                    │ │(对象存储)     │  │
                                                    │ └─────────────┘  │
                                                    └─────────────────┘

Node 规划 (生产, 最少3节点起步):
┌──────────────────────────────────────────────────┐
│ Node-1 (4C16G)          Node-2 (4C16G)           │
│ [api-gateway-1]         [api-gateway-2]           │
│ [ws-gateway-1]           [ws-gateway-2]            │
│ [auth-service-1]        [auth-service-2]          │
│ [user-service-1]        [user-service-2]          │
│ [room-service-1]        [room-service-2]          │
│ [wallet-service-1]      [wallet-service-2]         │
│ [redis-master]          [redis-replica]           │
├──────────────────────────────────────────────────┤
│ Node-3 (8C32G)                                   │
│ [game-engine-hongzhong-1..3]                      │
│ [game-engine-taojiang-1..3]                       │
│ [game-engine-paodekuai-1..3]                      │
│ [replay-storage]                                  │
│ [monitoring-stack(prometheus/grafana)]            │
│ [log-collector(fluentd)]                          │
└──────────────────────────────────────────────────┘
```

### 2.2 核心资源配额建议

#### 2.2.1 各服务 Pod 配置

| 服务 | CPU Request/Limit | Memory Request/Limit | 副本数 | HPA策略 |
|------|-------------------|---------------------|--------|---------|
| api-gateway | 500m / 1000m | 512Mi / 1Gi | 3 | CPU>70%→+1, max=5 |
| ws-gateway | 500m / 1000m | 512Mi / 1Gi | 3 | 连接数>3000/pod→+1 |
| auth-service | 200m / 500m | 256Mi / 512Mi | 2 | CPU>60%→+1 |
| user-service | 200m / 500m | 256Mi / 512Mi | 2 | — |
| room-service | 500m / 1000m | 512Mi / 1Gi | 3 | CPU>70%→+1 |
| game-engine-\* | 1000m / 2000m | 1Gi / 2Gi | 每游戏3个 | 自定义指标扩缩 |
| wallet-service | 200m / 500m | 256Mi / 512Mi | **2**(无状态只读) | — |
| replay-service | 500m / 1000m | 512Mi / 1Gi | 2 | — |
| risk-service | 300m / 700m | 384Mi / 768Mi | 2 | — |
| activity-service | 200m / 400m | 192Mi / 384Mi | 1 | — |
| notification-svc | 100m / 300m | 128Mi / 256Mi | 1 | — |
| admin-backend | 200m / 500m | 256Mi / 512Mi | 2 | — |
| file-service | 200m / 500m | 256Mi / 512Mi | 2 | — |
| config-service | 100m / 200m | 128Mi / 256Mi | 1 | — |
| monitor-agent | 50m / 100m | 64Mi / 128Mi | DaemonSet | — |

#### 2.2.2 基础设施组件规格

| 组件 | 规格 | 说明 |
|------|------|------|
| PostgreSQL | 4C16G, SSD云盘 500GB | 主从复制 + 自动故障转移 |
| Redis Cluster | 3主3从, 8G内存/节点 | 槽位分片16384, 持久化AOF |
| 消息队列(RabbitMQ/Pulsars) | 2C4G, 3节点镜像队列 | 异步任务解耦 |
| 对象存储(MinIO/OSS) | 按量付费 | 回放文件/音效资源/备份 |
| Prometheus | 2C4G | 监控时序数据30天 |
| Grafana | 1C2G | 可视化面板 |
| ELK/EFK Stack | 3C6G×2 | 日志收集与检索 |

### 2.3 关键 K8s 资源 YAML 示例

#### 2.3.1 API Gateway Deployment

```yaml
# k8s/overlays/prod/deployment/api-gateway.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: gateway-system
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
        app: api-gateway
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: api-gateway
                topologyKey: kubernetes.io/hostname
      containers:
        - name: api-gateway
          image: registry.example.com/game/api-gateway:v1.0.0
          ports:
            - containerPort: 8080
              name: http
            - containerPort: 9090
              name: metrics
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          envFrom:
            - secretRef:
                name: api-gateway-secrets
            - configMapRef:
                name: api-gateway-config
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
          volumeMounts:
            - name: tls-certs
              mountPath: /etc/tls
              readOnly: true
      volumes:
        - name: tls-certs
          secret:
            secretName: api-gateway-tls-certs
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: api-gateway-hpa
  namespace: gateway-system
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: api-gateway
  minReplicas: 3
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
            averageUtilization: 70
```

---

## 三、CI/CD 流水线设计

### 3.1 整体流水线架构

```
代码提交(Git)
    │
    ▼
┌─────────────────┐
│  代码质量门禁     │  ← 触发条件: PR/Merge到main分支
│  • Lint/Format   │
│  • 单元测试      │     耗时: ~5分钟
│  • 安全扫描     │
│  • 覆盖率>70%   │
└────────┬────────┘
         │ 通过
         ▼
┌─────────────────┐
│  构建镜像        │
│  • 多阶段构建    │     Docker BuildKit
│  • 镜像扫描     │     Trivy/Clair
│  • 推送Registry  │
└────────┬────────┘         耗时: ~8分钟
         │
    ┌────┴────┬──────────┐
    ▼         ▼          ▼
 [DEV]     [TEST]    [STAGING]
 部署       部署        部署
 自动       自动       需审批
              │
              ▼ (UAT通过)
           [PROD] 部署
           (需双重审批)
```

### 3.2 Jenkins/GitLab CI 流水线定义

```yaml
# .gitlab-ci.yml (或 Jenkinsfile)
stages:
  - lint-test
  - security-scan
  - build
  - deploy-dev
  - deploy-test
  - deploy-staging
  - deploy-prod

variables:
  DOCKER_REGISTRY: registry.example.com/game
  IMAGE_PREFIX: $DOCKER_REGISTRY/$CI_PROJECT_NAME

# ========== Stage 1: 代码质量 ==========
lint-and-test:
  stage: lint-test
  image: golang:1.21-alpine
  script:
    - go vet ./...
    - golint ./... | tee lint-report.txt
    - go test -race -coverprofile=coverage.out ./... | tee test-result.txt
    # 覆盖率门槛
    - |
      COVERAGE=$(go tool cover -func=coverage.out | grep total | awk '{print $3}' | tr -d '%')
      if [ $(echo "$COVERAGE < 70" | bc) -eq 1 ]; then
        echo "ERROR: Coverage $COVERAGE% is below 70%"
        exit 1
      fi
  artifacts:
    paths:
      - coverage.out
      - test-result.txt
      - lint-report.txt
    expire_in: 7 days
  only:
    - merge_requests
    - main

# ========== Stage 2: 安全扫描 ==========
security-scan:
  stage: security-scan
  image: aquasec/trivy:latest
  script:
    - trivy fs --severity HIGH,CRITICAL --format table --output scan-report.txt .
    - trivy fs --exit-code 1 --severity CRITICAL .
  artifacts:
    paths: [scan-report.txt]

# ========== Stage 3: 构建镜像 ==========
build-image:
  stage: build
  image: docker:24.0-dind
  services:
    - docker:24.0-dind
  variables:
    DOCKER_TLS_CERTDIR: "/certs"
  before_script:
    - echo $CI_REGISTRY_PASSWORD | docker login -u $CI_REGISTRY_USER --password-stdin $DOCKER_REGISTRY
  script:
    # 多阶段构建, 最终镜像仅含编译后二进制
    - docker build --build-arg VERSION=$CI_COMMIT_SHA \
                   --target production \
                   -t $IMAGE_PREFIX:$CI_COMMIT_SHA \
                   -t $IMAGE_PREFIX:latest .
    - docker push $IMAGE_PREFIX:$CI_COMMIT_SHA
    - docker push $IMAGE_PREFIX:latest
  only:
    - main

# ========== Stage 4~6: 分环境部署 ==========
deploy-dev:
  stage: deploy-dev
  image: bitnami/kubectl:latest
  script:
    - kubectl set image deployment/api-gateway api-gateway=$IMAGE_PREFIX:$CI_COMMIT_SHA -n dev
    - kubectl rollout status deployment/api-gateway -n dev --timeout=180s
  environment:
    name: dev
    url: https://dev-api.example.com
  only:
    - main

deploy-test:
  stage: deploy-test
  image: bitnami/kubectl:latest
  script:
    - kubectl set image deployment/api-gateway api-gateway=$IMAGE_PREFIX:$CI_COMMIT_SHA -n test
    - kubectl rollout status deployment/api-gateway -n test --timeout=300s
  environment:
    name: test
    url: https://test-api.example.com
  only:
    - main

deploy-staging:
  stage: deploy-staging
  image: bitnami/kubectl:latest
  script:
    - kubectl set image deployment/api-gateway api-gateway=$IMAGE_PREFIX:$CI_COMMIT_SHA -n staging
    - kubectl rollout status deployment/api-gateway -n staging --timeout=300s
  environment:
    name: staging
    url: https://staging-api.example.com
  when: manual  # 手动触发, UAT验证后
  only:
    - main

deploy-prod:
  stage: deploy-prod
  image: bitnami/kubectl:latest
  script:
    - kubectl set image deployment/api-gateway api-gateway=$IMAGE_PREFIX:$CI_COMMIT_SHA -n prod
    - kubectl rollout status deployment/api-gateway -n prod --timeout=600s
    # 通知发布成功
    - curl -X POST "$WEBHOOK_URL" -d '{"text":"✅ PROD部署成功: '$CI_COMMIT_SHA'"}'
  environment:
    name: production
    url: https://api.example.com
  when: manual  # 双重手动确认(技术负责人+产品)
  only:
    - main
```

### 3.3 Dockerfile 标准（多阶段构建）

```dockerfile
# ============ 构建阶段 ============
FROM golang:1.21-alpine AS builder

WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download

COPY . .
RUN CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build \
    -ldflags="-w -s -X main.Version=${VERSION} -X main.Commit=${COMMIT}" \
    -o /app/server cmd/server/main.go

# ============ 生产运行阶段 ============
FROM alpine:3.19 AS production

# 安装最小依赖(ca-certificates用于HTTPS请求, tzdata用于时区)
RUN apk add --no-cache ca-certificates tzdata && \
    rm -rf /var/cache/apk/* /tmp/*

# 创建非root用户
RUN addgroup -S appgroup && adduser -S appuser -G appgroup
USER appuser

WORKDIR /app

# 从builder阶段复制编译产物
COPY --from=builder /app/server .
COPY --from=builder /app/configs ./configs
COPY --from=builder /app/migrations ./migrations

# 健康检查端口
EXPOSE 8080

# 安全参数
HEALTHCHECK --interval=30s --timeout=5s --start-period=15s --retries=3 \
    CMD wget --no-verbose --tries=1 --spider http://localhost:8080/healthz || exit 1

ENTRYPOINT ["./server"]
```

### 3.4 发布检查清单 (Go-Live Checklist)

| 检查项 | 负责人 | 状态 | 备注 |
|--------|--------|------|------|
| 所有单元测试通过 | 开发者 | ☐ | CI绿色 |
| 代码Review完成 | Tech Lead | ☐ | 至少2人Approved |
| 安全扫描无CRITICAL | 安全接口人 | ☐ | Trivy扫描结果 |
| 数据库迁移脚本就绪 | DBA | ☐ | 已在Staging验证 |
| 配置项已同步到生产ConfigMap | DevOps | ☐ | K8s diff检查 |
| 回滚方案已准备 | DevOps | ☐ | 前一版本镜像可用 |
| 监控告警已配置 | 运维 | ☐ | 关键指标dashboard就绪 |
| 性能测试报告(QPS/P99) | QA | ☐ | 满足SLA要求 |
| 用户公告已发布 | 产品/运营 | ☐ | 维护通知+预计时长 |
| 客服团队已通知 | 客服主管 | ☐ | FAQ和升级预案 |
| 甲方负责人确认 | 甲方对接人 | ☐ | 邮件/工单书面确认 |

---

## 四、环境配置管理

### 4.1 ConfigMap 与 Secret 管理

```yaml
# k8s/base/configmap/api-gateway-config.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: api-gateway-config
data:
  # ===== 应用配置 =====
  APP_ENV: "production"
  APP_PORT: "8080"
  LOG_LEVEL: "info"

  # ===== 服务发现 =====
  AUTH_SERVICE_URL: "http://auth-service.game-services:8080"
  USER_SERVICE_URL: "http://user-service.game-services:8080"
  ROOM_SERVICE_URL: "http://room-service.game-services:8080"
  WALLET_SERVICE_URL: "http://wallet-service.game-services:8080"

  # ===== Redis =====
  REDIS_ADDR: "redis-cluster.infra-system:6379"
  REDIS_DB: "0"
  REDIS_POOL_SIZE: "100"

  # ===== 限流配置 =====
  RATE_LIMIT_GLOBAL: "10000"      # 全局QPS上限
  RATE_LIMIT_PER_USER: "60"        # 每用户每分钟请求数
  RATE_LIMIT_BURST: "10"           # 突发允许量

  # ===== JWT =====
  JWT_ISSUER: "game-platform"
  JWT_ACCESS_EXPIRE_HOURS: "2"
  JWT_REFRESH_EXPIRE_DAYS: "7"

  # ===== 风控 =====
  RISK_ENABLED: "true"
  RISK_ACTION_MODE: "detect_only"   # 只识别不操控!

  # ===== CORS =====
  CORS_ALLOW_ORIGINS: "https://admin.example.com"
---
apiVersion: v1
kind: Secret
metadata:
  name: api-gateway-secrets
type: Opaque
stringData:
  # ===== 数据库连接(加密存储) =====
  DB_DSN: "postgres://user:PASS@postgresql.infra-system:5432/gamedb?sslmode=require"

  # ===== JWT签名密钥(RS256私钥) =====
  JWT_PRIVATE_KEY: |
    -----BEGIN RSA PRIVATE KEY-----
    ... (base64 encoded key from Vault/KMS)
    -----END RSA PRIVATE KEY-----

  # ===== 接口签名密钥(每渠道独立) =====
  APP_SECRET_CHANNEL_OFFICIAL: "random-32-byte-secret-here"
  APP_SECRET_CHANNEL_HUAWEI: "another-random-32byte-secret-key"

  # ===== 对称加密密钥(AES-256 for field encryption) =====
  AES_ENCRYPTION_KEY: "256-bit-hex-encoded-aes-key-must-be-exact-length"

  # ===== 第三方服务凭证 =====
  SMS_API_KEY: "sms-provider-api-key"
  PUSH_API_SECRET: "push-notification-secret"

  # ===== 对象存储 =====
  OSS_ACCESS_KEY: "oss-access-key-id"
  OSS_SECRET_KEY: "oss-secret-access-key"
```

### 4.2 环境变量差异矩阵

| 变量名 | DEV | TEST | STAGING | PROD |
|--------|-----|------|---------|------|
| APP_ENV | development | test | staging | **production** |
| LOG_LEVEL | debug | info | warn | **warn** |
| DB_DSN | postgres://dev@localhost | postgres://test@test-db | postgres://readonly-stg@prod-ro | postgres://app@**prod-master** |
| REDIS_ADDR | localhost:6379 | redis-test:6379 | redis-staging:6379 | **redis-cluster:6379** |
| RATE_LIMIT_GLOBAL | 1000 | 5000 | 8000 | **10000** |
| RISK_ENABLED | false | true | true | **true** |
| RISK_AUTO_FREEZE | false | false | false | **false**(永远只标记!) |
| SENTRY_DSN | (dev sentry) | (test sentry) | (stage sentry) | **prod sentry** |

---

## 五、备份与恢复策略

### 5.1 备份策略总览

| 数据类型 | 备份方式 | 频率 | 保留期 | RTO | RPO |
|----------|---------|------|--------|-----|-----|
| PostgreSQL全量 | pg_dump + gzip + gpg加密 | **每日02:00** | 30天 | ≤4小时 | ≤24小时 |
| PostgreSQL增量 | WAL归档(S3/OSS) | 持续流式 | 7天 | — | ≤5分钟 |
| Redis RDB | BGSAVE → OSS | 每6小时 | 7天 | ≤1小时 | ≤6小时 |
| Redis AOF | AOF重写 → OSS | 每日 | 3天 | — | ≤1秒 |
| 对象存储文件 | 跨区域复制(CRR) | 实时 | 永久 | 即时 | 0 |
| K8s资源(etcd) | etcd快照 | 每小时 | 72小时 | ≤30分钟 | ≤1小时 |
| 配置(ConfigMap/Secret) | Git版本控制 | 每次变更 | 永久 | ≤10分钟 | 0 |
| 应用日志 | EFK归档→冷存储 | 每日轮转 | 90天 | — | — |

### 5.2 自动备份脚本 (PostgreSQL)

```bash
#!/bin/bash
# scripts/backup-postgresql.sh
set -euo pipefail

BACKUP_DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="/backup/postgresql/$BACKUP_DATE"
RETENTION_DAYS=30
GPG_RECIPIENT="backup@example.com"  # PGP公钥ID

mkdir -p "$BACKUP_DIR"

echo "[$(date)] 开始PostgreSQL全量备份..."

# 执行pg_dump (并行压缩, 4 jobs)
pg_dump -h "$DB_HOST" -p "$DB_PORT" -U "$DB_USER" \
        -d "$DB_NAME" \
        -Fc \                    # 自定义格式(支持并行恢复)
        -j 4 \                    # 4个并行job
        -f "$BACKUP_DIR/db.dump"

echo "[$(date)] dump完成, 大小: $(du -h "$BACKUP_DIR/db.dump" | cut -f1)"

# GPG对称加密
gpg --batch --yes --symmetric --cipher-algo AES256 \
    --output "$BACKUP_DIR/db.dump.gpg" \
    --passphrase "$GPG_PASSPHRASE" \
    "$BACKUP_DIR/db.dump"

# 删除未加密的原始dump (安全!)
rm -f "$BACKUP_DIR/db.dump"

# 上传到对象存储(OSS/S3)
aws s3 cp "$BACKUP_DIR/db.dump.gpg" \
          "s3://game-backups/prod/postgresql/$BACKUP_DATE/db.dump.gpg" \
          --storage-class STANDARD_IA

echo "[$(date)] 加密备份已上传到OSS"

# 清理本地临时文件
rm -rf "$BACKUP_DIR"

# 清理过期的远程备份 (保留RETENTION_DAYS天)
aws s3 ls "s3://game-backups/prod/postgresql/" | \
    while read -r line; do
        backup_date=$(echo "$line" | awk '{print $2}')
        backup_epoch=$(date -d "$backup_date" +%s 2>/dev/null || date -j -f "%Y-%m-%d" "$backup_date" +%s)
        cutoff_epoch=$(date -d "-$RETENTION_DAYS days" +%s 2>/dev/null || date -v-${RETENTION_DAYS}d +%s)

        if [ "$backup_epoch" -lt "$cutoff_epoch" ]; then
            echo "清理过期备份: $backup_date"
            aws s3 rm --recursive "s3://game-backups/prod/postgresql/$backup_date/"
        fi
    done

echo "[$(date)] 备份任务完成"
```

### 5.3 恢复演练计划

| 演练场景 | 频率 | 恢复步骤概要 | 验收标准 |
|----------|------|-------------|---------|
| 单表误删恢复 | 季度 | 从最新dump中恢复单表(pg_restore --table) | 数据完整, 业务无损 |
| 全库恢复 | 半年 | 新实例+导入最新dump+应用WAL | RTO<4h, RPO<24h |
| Redis数据丢失 | 季度 | 从RDB/AOF恢复到新集群 | 缓存预热完成, 无报错 |
| K8s集群灾难 | 年度 | 新集群部署+etcd恢复+应用重新部署 | 全部Pod Running, 服务可用 |
| 区域级故障 | 年度(桌面演练) | 切换到异地灾备集群 | DNS切换后业务恢复 |

### 5.4 灾备方案 (DR - Disaster Recovery)

```
                    主机房 (主集群)                     异地灾备机房
                  ┌──────────────────┐                ┌──────────────────┐
                  │  PROD K8s集群    │   异步复制     │  DR K8s集群      │
                  │  ┌────────────┐  │ ────────>     │  ┌────────────┐  │
                  │  │PostgreSQL │──┼──────────────>│─→│PG只读(待切) │  │
                  │  └────────────┘  │  WAL Streaming │  └────────────┘  │
                  │  ┌────────────┐  │                │  ┌────────────┐  │
                  │  │Redis Cluster│─┼──异步复制────>│─→│Redis(待切)  │  │
                  │  └────────────┘  │                │  └────────────┘  │
                  │  ┌────────────┐  │                │  ┌────────────┐  │
                  │  │OSS对象存储  │─┼──CRR跨域复制─>│─→│OSS副本     │  │
                  │  └────────────┘  │                │  └────────────┘  │
                  └──────────────────┘                └──────────────────┘

RTO目标: ≤ 4小时 (从触发切换到灾备集群完全接管)
RPO目标: ≤ 1小时 (最大可接受的数据丢失窗口)

切换决策链:
1. 自动检测: Prometheus告警(主集群不可达 > 5min)
2. 一线运维: 初步评估, 决定是否启动切换预案
3. 技术负责人: 审批切换操作
4. 甲方通知: 同步告知甲方技术负责人
5. 执行切换: DNS切流量到灾备集群 + PG提升为主
6. 验证: 核心接口冒烟测试 + 监控指标确认
7. 公告: 发布故障公告 + 预计恢复时间
```

---

## 六、监控体系

### 6.1 监控分层

| 层级 | 工具 | 监控内容 | 告警通道 |
|------|------|---------|---------|
| **基础设施** | Node Exporter + Grafana | CPU/内存/磁盘/网络/IOPS | PagerDuty/钉钉 |
| **K8s集群** | kube-state-metrics | Pod状态/节点健康/资源配额 | 钉钉+邮件 |
| **应用性能(APM)** | SkyWalking/Jaeger | 接口延迟(P50/P95/P99)/错误率/吞吐量 | 钉钉+短信 |
| **业务指标** | Prometheus自定义 | 在线人数/开房数/积分流转/异常牌局数 | 钉钉+电话 |
| **日志集中** | EFK Stack | 错误日志聚合/慢查询/访问日志 | 钉钉 |
| **安全审计** | Falco + Auditd | 异常系统调用/权限变更/登录异常 | 短信+电话 |

### 6.2 核心 SLIs/SLOs

| SLI (服务水平指标) | 目标SLO | 计算方式 | 告警阈值 |
|-------------------|---------|---------|---------|
| API可用性 | **99.9%** | 成功请求数/总请求数(排除客户端错误) | <99.5%持续5min |
| API响应时间P99 | **≤200ms** | 第99百分位响应时间(网关层) | >500ms持续3min |
| WS连接成功率 | **99.5%** | WS握手成功数/尝试连接数 | <98%持续5min |
| 游戏房间创建成功率 | **99.9%** | 房间创建成功数/总请求 | <99%持续3min |
| 积分交易一致性 | **100%** | (钱包写入=流水记录) 的比例 | **任何不一致即P0告警** |

### 6.3 关键 Dashboard 面板清单

| 面板名称 | 受众 | 核心图表 |
|----------|------|---------|
| **全局运营看板** | 产品/运营/甲方 | 在线趋势/游戏分布/收入趋势/新增用户/活跃度 |
| **技术总览面板** | 技术负责人/DevOps | 各服务QPS/错误率/延迟/Pod状态/资源使用率 |
| **数据库面板** | DBA/后端 | 连接数/慢查询/QPS/TPS/复制延迟/锁等待 |
| **缓存面板** | 后端/DevOps | 命中率/内存使用/键空间分布/主从同步/大key |
| **风控面板** | 风控组/安全 | 事件趋势/处理效率/风险等级分布/Top风险用户 |
| **告警总览面板** | 全员 | 当前活跃告警/历史告警趋势/MTTR(平均修复时间) |

---

## 七、网络拓扑与安全组

### 7.1 生产环境网络ACL

```
Internet
  │
  │ 仅开放: 443(HTTPS), 80(HTTP→301跳转)
  ▼
┌─────────────────────────────────────┐
│         Cloudflare CDN/WAF           │
│  DDoS防护 + WAF规则 + Bot管理       │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│        SLB (负载均衡器)              │
│  HTTPS终止 → 转发到 Ingress NodePort │
│  健康检查: /healthz                  │
└──────────────┬──────────────────────┘
               │ 仅 30443(Ingress HTTPS)
               ▼
┌─────────────────────────────────────┐
│     K8s Worker Nodes (安全组)        │
│                                     │
│  入站规则:                           │
│  ├─ 30443 ← SLB (Ingress)           │
│  ├─ 22   ← VPN/Bastion(管理SSH)     │
│  └─ 6443 ← K8s API Server(仅master) │
│                                     │
│  出站规则:                           │
│  ├─ 允许所有(出站不限制)             │
│  └─ NAT网关出口IP固定               │
│                                     │
│  Pod间网络 (CNI-Calico):            │
│  ├─ Namespace NetworkPolicy 隔离     │
│  ├─ 仅允许声明过的跨Namespace访问     │
│  └─ 拒绝所有其他跨NS通信             │
└─────────────────────────────────────┘
```

### 7.2 关键端口清单

| 用途 | 协议 | 端口 | 暴露范围 |
|------|------|------|---------|
| HTTPS(API) | TCP | 443 | 公网(经CDN) |
| WebSocket(WSS) | TCP | 443(同上) | 公网(同上) |
| SSH管理 | TCP | 22 | 仅VPN内网/Bastion |
| K8s API Server | TCP | 6443 | 仅Master节点间 |
| PostgreSQL | TCP | 5432 | **仅K8s内部**, 不对外 |
| Redis | TCP | 6379 | **仅K8s内部**, 不对外 |
| RabbitMQ | TCP | 5672, 15672 | **仅K8s内部**, 15672仅VPN |
| Grafana | TCP | 3000 | 仅VPN/内网 |
| Prometheus | TCP | 9090 | 仅VPN/内网 |

---

## 八、运维规范

### 8.1 发布窗口

| 环境 | 发布窗口 | 禁止时段 |
|------|---------|---------|
| PROD | **周二/四 10:00-12:00** | 周五晚~周一早(高峰), 法定节假日 |
| STAGING | 工作日 09:00-18:00 | 无强制限制 |
| TEST | 任意时间(自动部署) | 无 |
| DEV | 任意时间 | 无 |

### 8.2 变更管理规范

| 变更类型 | 审批流程 | 回滚条件 |
|----------|---------|---------|
| **常规功能上线** | MR Review + Tech Lead Approve + 发布Checklist | 错误率>1% 或 P99>1s 持续5min |
| **数据库DDL变更** | DBA审核 + 测试环境先执行 + 备份 | 影响行数>预期 or 锁表超时 |
| **配置变更(ConfigMap)** | DevOps Review + Diff确认 | 相关接口错误率上升 |
| **紧急Hotfix** | 事后补单(48h内) | 同常规 |
| **扩缩容操作** | DevOps独立操作 + 通知群 | 资源不足告警未消除 |
| **证书更新** | 提前30天提醒 + 窗期内更新 | — (Let's Encrypt自动续期优先) |

### 8.3 On-Call 值班制度

| 角色 | 响应SLA | 值班工具 | 升级链路 |
|------|---------|---------|---------|
| **一线值班(L1)** | P1:<5min, P2:<15min, P3:<2h | PagerDuty/钉钉群 | 15min无响应→升级L2 |
| **二线专家(L2)** | P1:<10min, P2:<30min | 电话+短信 | 30min无响应→升级Tech Lead |
| **技术负责人** | P1随时待命 | 电话直接 | 必须响应 |
| **甲方联系人** | P1事件同步 | 微信/电话 | 信息同步, 决策协助 |

### 8.4 故障分级与处理模板

#### P0 - 系统完全不可用

```
影响: 全部用户无法正常游戏(无法登录/匹配/结算)
响应: < 5分钟
处理:
  1. 立即在事故频道通报: @here [P0] XXX服务不可用, 正在排查
  2. 快速定位: 检查监控面板 → 确认影响范围
  3. 应急措施: 若短时间内无法修复 → 准备回滚到上一稳定版本
  4. 执行回滚: kubectl rollout undo deployment/xxx -n prod
  5. 验证恢复: 核心接口冒烟测试
  6. 同步甲方: 电话通知甲方技术负责人当前状态和预计恢复
  7. 恢复后: 发送事故初步报告(影响时长/影响用户数/根因初步判断)
  8. 24小时内: 输出完整RCA(Root Cause Analysis)报告
```

---

*文档结束*
